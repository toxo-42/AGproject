# PeOb 프로젝트 작업 규칙

## 진행상황 기록 (필수)
- **`진행상황_및_로드맵.md`는 이 프로젝트의 "일기장"이다. 의미 있는 변경을 할 때마다 항상 함께 업데이트한다.**
  - 새 기능/모듈을 완료하면 → "2. 지금까지 완료한 것 ✅" 에 반영
  - 로드맵 항목을 진행/완료하면 → 해당 Phase의 체크박스(`[ ]`→`[x]`) 갱신
  - 새로 정해진 의사결정/임시값은 → "5. 핵심 의사결정" 또는 "6. 알려진 TODO"에 기록
  - 문서 상단 날짜/최종 목표는 방향이 바뀌지 않는 한 유지
- 커밋에 코드 변경이 포함되면, 관련된 진행상황 문서 갱신도 같은 흐름에서 처리한다(빠뜨리지 말 것).

## 프로젝트 개요
- 최종 목표: 스마트폰 앱에서 ML 모델이 여러 사용자의 페달 압력 데이터를 학습해,
  각자 운전 습관에 맞춰 경고 임계값이 가변되는 **개인화 오조작(급발진 오인) 감지**.
- 데이터 흐름: STM32(FSR) → UART → ESP32-C3 → BLE NUS Notify → Android 앱
  → `BleService`(RAW 디코드→윈도우 버퍼링) → Chaquopy `judge.py` 판정 → 경고.
- 오조작 판단은 **전부 폰 로컬**. 펌웨어는 raw 센서값만 전달.
- PC 프로토타입(`prototype/`)과 앱이 **같은 `judge.py`를 공유**한다(로직은 여기만 갈아끼움).

## 관련 문서
- `진행상황_및_로드맵.md` — 진행상황·로드맵(항상 최신 유지)
- `BLE_NUS_마이그레이션.md` — BLE 채널
- `BLE_RAW_스트림_규격.md` — RAW 프레임 포맷
- `코드구조_공부자료.md` — 코드 구조 학습 자료
