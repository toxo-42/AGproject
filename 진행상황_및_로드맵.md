# PeOb 진행상황 & 로드맵

> 작성일: 2026-06-25
> 최종 목표: **스마트폰 앱에서 ML 모델이 여러 사용자의 페달 압력 데이터를 학습해,
> 각자의 운전 습관에 맞춰 경고 임계값이 가변되는 개인화 오조작 감지.**
>
> 관련 문서: `BLE_NUS_마이그레이션.md`(BLE 채널) · `BLE_RAW_스트림_규격.md`(RAW 포맷) · `코드구조_공부자료.md`

---

## 1. 시스템 전체 그림

```
STM32(FSR 페달센서) ──UART 200Hz──▶ ESP32-C3 ──BLE NUS Notify 50Hz(4샘플 배치)──▶ Android 앱
                                                                                    │
                                          ┌─────────────────────────────────────────┘
                                          ▼
                              [BleService] RAW 디코드 → 윈도우 버퍼링
                                          ▼
                              [Chaquopy] Python judge.py 로 판정
                                          ▼
                              misop=true → 음성/헤드업 경고
```

- **오조작 판단은 전부 폰 로컬에서 수행.** 펌웨어는 raw 센서값만 전달(시각·판정 개념 없음).
- 폰에 Python 런타임을 심어(Chaquopy) 판정 로직을 Python으로 작성 → PC 프로토타입(`prototype/`)과 앱이 **같은 judge.py 공유**.

---

## 2. 지금까지 완료한 것 ✅

### 2-1. Python on Android (Chaquopy) — **검증 완료**
- `app/build.gradle.kts` + root `build.gradle.kts`에 `com.chaquo.python` 17.0.0 도입, Python 3.12.
- `Python.start(AndroidPlatform)` → `getModule("judge").callAttr("judge_json", ...)` 경로가 **실기기에서 동작 확인됨**(자가진단 버튼 → "자가진단 OK" 토스트).
- **핵심 교훈**: Chaquopy는 Java `ArrayList`를 Python `list`로 자동 변환하지 않음
  → `'ArrayList' object is not iterable` 발생. **JSON 문자열을 경계로 주고받아** 해결.
  - Kotlin: `JSONArray(...).toString()` → Python: `json.loads(...)`.
- 디버그용 **자가진단 버튼**(`MainActivity` 우상단)으로 BLE 없이 단독 검증 가능.

### 2-2. BLE RAW 스트림 디코더 — **규격대로 재작성 완료**
`BLE_RAW_스트림_규격.md` 기준으로 `BleService` 정비:
- 배치 프레임 파싱: `count(1B)` + `RawSample × N`, 프레임 길이 검증(`1 + N*10`).
- 샘플 10B(little-endian): `int32 Q31 break` + `int32 Q31 accel` + `uint16 cnt`.
  - 환산: `normalized = raw / 2^31` (0.0~1.0 정규화 힘).
- `judge.py`가 기대하는 `[accel, brake]` 순서로 push.
- `cnt` 기반 누락 검출(wrap-around 처리, 현재는 경고 로그만).
- **원시 샘플 CSV 로깅**(Phase A 착수) — 아래 §4 Phase A 참고.
- notify 구독 **전에** `requestMtu(247)` + 연결 후 `CONNECTION_PRIORITY_HIGH`
  (MTU 미요청 시 23으로 남아 41B 프레임 수신 불가 → 데이터 0의 1순위 원인).
- 시간동기(16B) 전송 제거 — 펌웨어에서 절대시각 경로 삭제됨.

### 2-3. 현재 판정 로직 (`app/src/main/python/judge.py`)
```python
ACCEL_HIGH = 0.85   # 엑셀 깊게 밟음 기준
BRAKE_LOW  = 0.10   # 브레이크 거의 안 밟음 기준
HIGH_RATIO = 0.5    # 윈도우 내 '엑셀↑+브레이크↓' 비율이 이 이상이면 오조작
```
- 윈도우(기본 50샘플) 안에서 "엑셀 깊게 + 브레이크 안 밟음" 비율이 `HIGH_RATIO` 이상이면 오조작.
- ⚠️ **고정 임계값 휴리스틱.** 모든 사람·모든 상황에 같은 기준 → **개인화 없음.**

---

## 3. 현재 위치 vs 최종 목표 (Gap)

| 구분 | 지금 | 최종 목표 |
|---|---|---|
| 판정 방식 | 고정 임계값 휴리스틱 | **학습된 ML 모델** |
| 개인화 | 없음 (모두 동일 기준) | **사용자별 운전 습관 반영** |
| 임계값 | 코드 상수 | **데이터로부터 가변** |
| 학습 데이터 | 없음 | **여러 사용자의 페달 압력 시계열** |
| 적응 | 없음 | 사용 중 누적 데이터로 **온라인 갱신** |

→ 지금은 **"데이터 파이프라인(BLE→디코드→윈도우→Python)"이 뚫린 상태**.
   이 위에 학습/개인화 레이어를 얹는 것이 다음 단계.

---

## 4. 최종 목표까지 로드맵

### Phase A — 데이터 수집 기반 (선행 필수)
판정 이전에 **학습할 데이터를 모을 수 있어야** 함.
- [x] RAW 원시 샘플을 폰 로컬에 **CSV 로깅** (2026-07-02) — **실기기 검증 완료 (2026-07-10)**.
  - `BleService`가 **첫 RAW 샘플 수신 시점에 lazy 로 세션 파일 생성** (연결 실패 시 빈 파일 안 남김).
  - 저장 위치: `filesDir/logs/pedal_yyyyMMdd_HHmmss.csv` (권한 불필요, `adb pull`로 회수).
  - 컬럼: `recv_epoch_ms,cnt,accel,brake` — 판정 윈도우와 무관하게 **원시 샘플 전부** 1줄씩.
  - accel/brake 소수 4자리 반올림. 배치마다 flush, `onDestroy`에서 close.
  - 크기 감: 50Hz 기준 ~7MB/시간 → 세션별 파일 분리로 관리(수백 시간 축적 시 gzip 고려).

  **검증 결과 (176초 연속 주행, 35,292샘플):**
  | 항목 | 측정값 | 판정 |
  |---|---|---|
  | 샘플레이트 | 200.2 Hz | 규격(50Hz 배치 × 4샘플 = 200Hz)과 일치 ✅ |
  | `cnt` 불연속 | 0곳 | 샘플 누락 0% ✅ |
  | `cnt` 중복 | 0개 | ✅ |
  | accel/brake 범위 | 0.0000 ~ 1.0000 | Q31 환산 정상, 실제 페달 입력 기록됨 ✅ |

  → MTU 247 + `CONNECTION_PRIORITY_HIGH` 조합으로 **200Hz 무손실 수신이 실증됨.**

  **검증 중 발견·수정한 버그 (2026-07-10):**
  - `onDestroy()`가 `closeCsvWriter()` → `disconnectGatt()` 순서라, 종료 직후 도착한 notify 콜백이
    `ensureCsvWriter()`를 다시 타서 **4샘플짜리 고아 세션 파일이 생기고 닫히지도 않음**.
    → **`disconnectGatt()`를 먼저** 호출하도록 순서 교체 + `csvClosed` 플래그로 되살아나기 차단.
  - BLE notify 콜백이 **여러 바인더 스레드**에서 올라오는데 `BufferedWriter`는 thread-safe 가 아니고
    `ensureCsvWriter()`는 check-then-act 경쟁 상태였음 → `csvLock` 으로 전 구간 `synchronized`.
- [ ] **사용자(운전자) 식별** 개념 도입 — 누구의 데이터인지 라벨링.
  - 앱에 프로필 선택, 혹은 등록된 기기(`targetAddress`)를 사용자 키로.
- [ ] 정상 주행 vs 오조작 구간 라벨링 방법 결정(수동 태깅 / 이벤트 기반).

### Phase B — 오프라인 모델링 (PC `prototype/`)
폰에 올리기 전에 PC에서 알고리즘을 먼저 확정.
- [ ] 페달 압력 시계열에서 **개인별 특징(feature)** 추출
  (예: 평소 엑셀/브레이크 분포, 전환 패턴, 반응시간, 페달 동시 가압 빈도).
- [ ] **운전 습관 3단계 프로필 분류기** 학습 — 위 feature → `강함 / 보통 / 약함`.
- [ ] 프로필 → 임계값 세트 매핑 테이블 확정
  (`강함`은 평소 엑셀을 깊게 밟으므로 `ACCEL_HIGH`를 올려 오경보를 줄이고, `약함`은 낮춤).
- [ ] `judge.py`를 **프로필 인자를 받는 형태로 일반화** (프로필 미지정 시 `보통` = 현재 휴리스틱).

### Phase C — 폰 온디바이스 추론
- [ ] 학습된 분류기 아티팩트(가중치/통계)를 앱에 번들 or 로컬 저장.
- [ ] Chaquopy로 추론 (numpy/scikit-learn 등 Chaquopy 호환 패키지 확인 필요)
  또는 매우 가벼우면 순수 Python/Kotlin.
- [ ] 주행 초기 N초 데이터로 프로필을 판정 → 해당 프로필의 **임계값 세트 로드**.
- [ ] 사용자 프로필을 저장해 다음 주행에 재사용(매번 재분류 X).

### Phase D — 개인화 적응 (후순위)
> 사용자 확정 방향은 **3단계 프로필 분류**이며, 아래 연속 적응은 그 이후 확장 과제.
- [ ] 주행 데이터가 쌓이면 프로필 재판정(예: `보통`→`강함` 승격).
- [ ] (선택) 프로필 내에서도 개인 보정치를 두어 오경보 추가 감소.
- [ ] (선택) 여러 사용자 데이터를 모아 **공통 모델 + 개인 보정**(federated/transfer) 구조로 확장.

---

## 5. 핵심 의사결정 메모

### 확정됨 ✅
- **개인화 방식 (2026-07-10 확정)**: 연속적인 온라인 적응이 아니라,
  **운전 습관 3단계 프로필(`강함` / `보통` / `약함`) 분류 → 프로필별 임계값 세트 적용**.
  - 즉 모델의 출력은 "이 사람은 어느 프로필인가"이고, 임계값은 그 프로필에 매핑된 상수 세트.
  - `judge.py`의 `ACCEL_HIGH` / `BRAKE_LOW` / `HIGH_RATIO` 를 프로필별로 3세트 두는 형태가 자연스러움.
- **모델 종류**: 3-class 분류(지도). 이상치 탐지(비지도)는 채택하지 않음.
- **학습 위치**: PC(`prototype/`)에서 학습 → 폰은 Chaquopy로 추론만.

### 아직 정해야 할 것
- **프로필 라벨을 어떻게 얻나**: 자가 신고(설문)? 수집 데이터의 통계치로 사후 구간화(예: 평균 엑셀 압력 삼분위)?
- **프로필 판정 시점**: 주행 초기 N초? 첫 주행 전체? N은 얼마?
- **Chaquopy 패키지 제약**: numpy/sklearn 번들 가능 여부·앱 용량 영향 확인 필요.
  (3-class 분류 정도면 학습된 계수만 하드코딩해 **순수 Python**으로도 충분할 가능성 큼 → 용량 이득)
- **프라이버시**: 페달 데이터는 개인 운전 습관 → 로컬 저장 원칙, 외부 전송 시 동의/익명화.

---

## 6. 알려진 TODO / 임시값
- `RAW` 특성 UUID(`...6E400004`)는 **임시** — 펌웨어 확정 시 `BleService.companion object`와 펌웨어 `BLE.h` 동시 교체.
- 디버그 자가진단 버튼/`selfTestJudge()`는 검증용 → 안정화 후 제거 가능.
- `judge.py` 임계값(`ACCEL_HIGH` 등)은 실데이터 튜닝 전 임시.
- ~~`cnt` 보간/누락 보정 미구현~~ — 176초/35,292샘플 실측에서 **누락 0%** 확인(2026-07-10).
  현재 조건(MTU 247 + HIGH priority)에선 보정 불필요. 장시간 주행에서 재확인만 하면 됨.
- 빌드 검증은 Android Studio에서 수행(레포에 gradle wrapper 없음).
