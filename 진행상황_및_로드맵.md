# PeOb 진행상황 & 로드맵

> 작성일: 2026-06-25
> 최종 목표: **스마트폰 앱에 소형 AI 모델이 여러 사용자의 페달 압력 데이터를 학습해,
> 각자의 운전 습관에 맞춰 경고 임계값이 가변되는 개인화 오조작 감지.**
>
> 관련 문서: `BLE_NUS_마이그레이션.md`(BLE 채널) · `BLE_RAW_스트림_규격.md`(RAW 포맷) · `코드구조_공부자료.md`

---

## 1. 시스템 전체 그림

```
STM32(FSR 페달센서) ──UART 200Hz──▶ ESP32-C3 ──BLE NUS Notify 50Hz(4샘플 배치)──▶ Android 앱
                                                                                    │
                                          ┌─────────────────────────────────────────┘
                                          ▼
                              [BleService] RAW 디코드 → 윈도우 버퍼링
                                          ▼
                              [Chaquopy] Python judge.py 로 판정
                                          ▼
                              misop=true → 음성/헤드업 경고
```

- **오조작 판단은 전부 폰 로컬에서 수행.** 펌웨어는 raw 센서값만 전달(시각·판정 개념 없음).
- 폰에 Python 런타임을 심어(Chaquopy) 판정 로직을 Python으로 작성 → PC 프로토타입(`prototype/`)과 앱이 **같은 judge.py 공유**.

---

## 2. 지금까지 완료한 것 ✅

### 2-1. Python on Android (Chaquopy) — **검증 완료**
- `app/build.gradle.kts` + root `build.gradle.kts`에 `com.chaquo.python` 17.0.0 도입, Python 3.12.
- `Python.start(AndroidPlatform)` → `getModule("judge").callAttr("judge_json", ...)` 경로가 **실기기에서 동작 확인됨**(자가진단 버튼 → "자가진단 OK" 토스트).
- **핵심 교훈**: Chaquopy는 Java `ArrayList`를 Python `list`로 자동 변환하지 않음
  → `'ArrayList' object is not iterable` 발생. **JSON 문자열을 경계로 주고받아** 해결.
  - Kotlin: `JSONArray(...).toString()` → Python: `json.loads(...)`.
- 디버그용 **자가진단 버튼**(`MainActivity` 우상단)으로 BLE 없이 단독 검증 가능.

### 2-2. BLE RAW 스트림 디코더 — **규격대로 재작성 완료**
`BLE_RAW_스트림_규격.md` 기준으로 `BleService` 정비:
- 배치 프레임 파싱: `count(1B)` + `RawSample × N`, 프레임 길이 검증(`1 + N*10`).
- 샘플 10B(little-endian): `int32 Q31 break` + `int32 Q31 accel` + `uint16 cnt`.
  - 환산: `normalized = raw / 2^31` (0.0~1.0 정규화 힘).
- `judge.py`가 기대하는 `[accel, brake]` 순서로 push.
- `cnt` 기반 누락 검출(wrap-around 처리, 현재는 경고 로그만).
- **원시 샘플 CSV 로깅**(Phase A 착수) — 아래 §4 Phase A 참고.
- notify 구독 **전에** `requestMtu(247)` + 연결 후 `CONNECTION_PRIORITY_HIGH`
  (MTU 미요청 시 23으로 남아 41B 프레임 수신 불가 → 데이터 0의 1순위 원인).
- 시간동기(16B) 전송 제거 — 펌웨어에서 절대시각 경로 삭제됨.

### 2-3. 현재 판정 로직 (`judge.py`) — **프로필별 임계값 분기 완료 (2026-07-10)**
```python
PROFILES = {
    "strong": {"accel_high": 0.92, "brake_low": 0.15, "high_ratio": 0.55},
    "normal": {"accel_high": 0.85, "brake_low": 0.10, "high_ratio": 0.50},
    "weak":   {"accel_high": 0.75, "brake_low": 0.07, "high_ratio": 0.45},
}
def judge(samples, profile="normal") -> dict: ...
def judge_json(samples_json, profile="normal") -> str: ...
```
- 윈도우 안에서 "엑셀 깊게 + 브레이크 안 밟음" 비율이 `high_ratio` 이상이면 오조작.
- **설계 원칙**: "이 사람이 어느 프로필인가"(프로필 판정)와 "이 윈도우가 오조작인가"(오조작 판정)를 분리.
  프로필을 통계 규칙으로 정하든 학습된 분류기로 정하든 `judge()`는 바뀌지 않는다.
- `profile` 기본값이 있어 **기존 인자 1개 호출부와 호환**. 모르는 프로필명은 `normal` fallback.
- 검증: `accel=0.80` 윈도우 → `weak`만 경고, `normal`/`strong`은 미경고 (임계값 가변 실증).
- `prototype/src/judge.py` 와 `app/src/main/python/judge.py` 는 **바이트 단위로 동일**해야 한다.
- ⚠️ `PROFILES` 값은 전부 **실데이터 튜닝 전 임시값**.

---

## 3. 현재 위치 vs 최종 목표 (Gap)

| 구분 | 지금 | 최종 목표 |
|---|---|---|
| 판정 방식 | 고정 임계값 휴리스틱 | 경량 분류기(휴리스틱은 fallback) |
| 개인화 | **프로필별 임계값 분기 완료** (프로필 판정은 아직 수동) | 캘리브레이션 주행으로 **프로필 자동 판정** |
| 임계값 | **프로필별 3세트** (임시값) | **실데이터로 튜닝된 값** |
| 학습 데이터 | 원시 CSV만 (라벨 없음) | **라벨된 페달 압력 시계열** |

→ 데이터 파이프라인(BLE→디코드→윈도우→Python)이 뚫렸고,
   `judge.py`에 **프로필 경계**까지 생겼다. 남은 건 ①라벨 수집 ②프로필 자동 판정.

---

## 4. 최종 목표까지 로드맵

### Phase A — 데이터 수집 기반 (선행 필수)
판정 이전에 **학습할 데이터를 모을 수 있어야** 함.
- [x] RAW 원시 샘플을 폰 로컬에 **CSV 로깅** (2026-07-02) — **실기기 검증 완료 (2026-07-10)**.
  - `BleService`가 **첫 RAW 샘플 수신 시점에 lazy 로 세션 파일 생성** (연결 실패 시 빈 파일 안 남김).
  - 저장 위치: `filesDir/logs/pedal_yyyyMMdd_HHmmss.csv` (권한 불필요, `adb pull`로 회수).
  - 컬럼: `recv_epoch_ms,cnt,accel,brake,label` — 판정 윈도우와 무관하게 **원시 샘플 전부** 1줄씩.
  - accel/brake 소수 4자리 반올림. 배치마다 flush, `onDestroy`에서 close.
  - 크기 감: 50Hz 기준 ~7MB/시간 → 세션별 파일 분리로 관리(수백 시간 축적 시 gzip 고려).

  **검증 결과 (176초 연속 주행, 35,292샘플):**
  | 항목 | 측정값 | 판정 |
  |---|---|---|
  | 샘플레이트 | 200.2 Hz | 규격(50Hz 배치 × 4샘플 = 200Hz)과 일치 ✅ |
  | `cnt` 불연속 | 0곳 | 샘플 누락 0% ✅ |
  | `cnt` 중복 | 0개 | ✅ |
  | accel/brake 범위 | 0.0000 ~ 1.0000 | Q31 환산 정상, 실제 페달 입력 기록됨 ✅ |

  → MTU 247 + `CONNECTION_PRIORITY_HIGH` 조합으로 **200Hz 무손실 수신이 실증됨.**

  **검증 중 발견·수정한 버그 (2026-07-10):**
  - `onDestroy()`가 `closeCsvWriter()` → `disconnectGatt()` 순서라, 종료 직후 도착한 notify 콜백이
    `ensureCsvWriter()`를 다시 타서 **4샘플짜리 고아 세션 파일이 생기고 닫히지도 않음**.
    → **`disconnectGatt()`를 먼저** 호출하도록 순서 교체 + `csvClosed` 플래그로 되살아나기 차단.
  - BLE notify 콜백이 **여러 바인더 스레드**에서 올라오는데 `BufferedWriter`는 thread-safe 가 아니고
    `ensureCsvWriter()`는 check-then-act 경쟁 상태였음 → `csvLock` 으로 전 구간 `synchronized`.
- [x] **전용 데이터 수집 화면** `DataCollectActivity` (2026-07-10).
  - 진입: 설정(`DeviceManagerActivity`) → "데이터 수집" 버튼.
  - **실시간 그래프**(`PedalGraphView`, 커스텀 Canvas View — 외부 차트 라이브러리 없음):
    최근 5초(200Hz × 1000샘플), Y축 0.0~1.0 고정, accel(민트)·brake(하늘) 라인.
  - **붉은 실선 = `accel_high`, 붉은 점선 = `brake_low`** — 지금 밟는 세기가 임계값 대비 어디인지 즉시 보임.
  - 임계값은 Kotlin 하드코딩이 아니라 `judge.py`의 `thresholds_json(profile)` 에서 읽어온다
    (**단일 소스**: `PROFILES` 를 튜닝하면 그래프의 붉은 선도 자동으로 따라 움직임).
  - 프로필을 바꾸면 임계선이 즉시 갱신된다.
  - 상단에 라벨링 표시등(`○ 정상` / `● 오조작 기록 중`).
  - 실시간 스트림(50Hz 브로드캐스트)은 **이 화면이 떠 있는 동안만** 켜진다(`ACTION_SET_LIVE_STREAM`).
    화면을 벗어나면 스트림도 라벨링도 자동으로 꺼진다.
- [x] **세션 프로필 선택** (2026-07-10) — 수집 화면의 3단 토글(강함/보통/약함).
  - `SharedPreferences("AgPrefs")`의 `DRIVER_PROFILE` 에 저장.
  - `BleService`가 **감시 시작 시점에 한 번만** 읽어 세션 내내 고정(`sessionProfile`).
  - CSV 파일명에 기록: `pedal_yyyyMMdd_HHmmss_<profile>.csv`
  - **선택된 프로필이 `judge_json` 에 전달되어 실제로 임계값이 갈린다.**
- [x] **오조작 구간 수동 라벨링** (2026-07-10, `cf747a0`에서 UI 제거 후 **2026-07-13 개발자 전용 경로로 재도입**).
  - `DataCollectActivity` → `BleService`로 `ACTION_SET_LABEL` 인텐트 전송.
  - CSV에 `label` 컬럼 추가: `0`=정상, `1`=오조작. 배치(20ms) 단위로 같은 값이 찍힌다.
  - 수집 화면을 벗어나면 자동으로 꺼진다(다음 세션에 딸려가지 않도록).
  - ⚠️ **라벨과 실제 페달 파형의 타이밍이 맞아야 한다.** 2026-07-10 첫 수집에선
    버튼만 누르고 페달을 안 밟아 `label=1` 구간의 accel 이 0.001 이었다(학습 불가).
    그래서 그래프를 보며 라벨링하도록 수집 화면을 만든 것.
  - **2026-07-13 재도입**: 일반 사용자 화면에는 노출하지 않고, `DeviceManagerActivity`의
    "데이터 수집" 버튼을 **길게 누르면**(`EXTRA_DEV_MODE=true`) `DataCollectActivity`가
    라벨 토글 버튼(`btnLabelMisop`, 기본 `visibility="gone"`)을 보여주는 방식으로 부활시켰다.
    로지스틱 회귀(오조작 판정 모델) 학습용 라벨 데이터를 다시 모을 수 있게 하기 위함
    — 백엔드(`BleService`의 `ACTION_SET_LABEL`/CSV `label` 컬럼)는 UI 제거 이후에도 그대로
    남아 있어서 화면 쪽만 복구하면 됐다.
  - **2026-07-13 추가**: 같은 dev mode 화면에 **강/보통/약 스타일 라벨링도 함께 부활**.
    dev mode 진입 시 "패턴 파악"(`btnCalibrate`)/초기화 버튼은 `INVISIBLE`로 숨기고(레이아웃
    제약이 흔들리지 않도록 `GONE` 대신 `INVISIBLE` 사용, 그 자리에 `RadioGroup(rgStyle)`을
    똑같은 위치·크기로 겹쳐 배치), 대신 강/보통/약 3버튼 라디오그룹을 노출한다.
    - `BleService`에 `ACTION_SET_STYLE`/`EXTRA_STYLE`(`STYLE_STRONG/NORMAL/WEAK/UNSET`)과
      `@Volatile currentStyle` 추가, CSV에 `style` 컬럼 신설(`label` 컬럼처럼 행마다 찍힘,
      **파일명이 아니라 컬럼**으로 넣은 이유: 기존 설계(2026-07-10, 파일명 `_<profile>.csv`
      접미사)는 BLE 연결 시점에 CSV가 lazy 생성되는 것보다 스타일 선택이 항상 늦어서
      파일명에 반영이 안 되는 타이밍 문제가 있었음 — 컬럼 방식은 `label`처럼 세션 중간에
      스타일을 바꿔도 바로 반영되고 타이밍 문제가 없다).
    - **주의**: 지금 이 스타일 값은 **판정 로직(임계값)에 전혀 영향을 주지 않는다** — 순수히
      오프라인 feature 비교/학습 데이터 태깅용. 실시간 판정은 여전히 캘리브레이션
      (`calibratedThresholdsJson`)에만 의존한다. 즉 dev mode 화면에서는 캘리브레이션을 할 수
      없으므로(버튼 숨김) 그 세션 동안은 실시간 오조작 경고가 이전 캘리브레이션 값 그대로
      유지되거나(캘리브레이션 전이면) 아예 꺼져 있다 — 의도된 동작(라벨링 방해 안 되게).
    - 화면을 벗어나면(`onPause`) 오조작 라벨과 함께 스타일도 자동으로 `STYLE_UNSET`으로 복귀.
- [x] 디버그용 자가진단 버튼/`selfTestJudge()` 제거 (실데이터 연동이 검증되어 역할 종료).

> ⚠️ CSV 스키마가 4컬럼 → 5컬럼으로 바뀌었다. 2026-07-10 이전에 수집한 파일은
> `label` 컬럼이 없으므로 파서에서 구분 필요.

### Phase B — 오프라인 모델링 (PC `prototype/`)
폰에 올리기 전에 PC에서 알고리즘을 먼저 확정.
- [x] **샘플레이트/윈도우 불일치 정리** (2026-07-13).
      `synth.py`의 `SAMPLE_RATE_HZ`를 50→200으로 맞추고, `run.py`의
      `WINDOW_SIZE`를 `SAMPLE_RATE_HZ // 4`(=50샘플=0.25초, `BleService.WINDOW_SIZE`와 동일)로,
      `STRIDE`도 `WINDOW_SIZE`와 같게(겹치지 않는 tumbling 윈도우, 앱과 동일 방식) 변경.
      → 이제 프로토타입에서 튜닝한 `high_ratio`를 앱에 그대로 옮겨도 된다.
      `uv run python src/run.py`로 정상/오조작 케이스 재검증 완료(800샘플=4초×200Hz, 16윈도우).
- [x] 페달 압력 시계열에서 **개인별 특징(feature)** 추출 1차 구현 (2026-07-13).
  `prototype/src/features.py` 신규. `judge()`(0.25초 윈도우 단위)와 달리 세션 전체를
  받아 요약 통계를 낸다: `accel_mean/std/p90`, `brake_mean/std`, `max_accel_jump`(급전환
  강도), `simultaneous_press_ratio`(동시 가압 비율). 합성데이터로 sanity check —
  정상 주행 vs 오조작에서 `accel_p90`(0.50→0.98), `max_accel_jump`(0.06→0.64) 뚜렷하게 구분됨.
  - ⚠️ "반응시간"(브레이크 개시~깊게 밟기까지 시간)은 이벤트 구간 정의가 필요해 보류 — 실데이터
    보면서 설계하기로 결정.
  - ⚠️ **feature 값과 프로필(강/보통/약)의 실제 상관관계는 아직 검증 전.** 여기 있는 건
    "무엇을 측정할지"이지 "어느 값이 strong인지" 기준은 아니다. 기준값은 실데이터 모으면서
    튜닝 예정(순환논리 주의 — §6 TODO 참고).
- [x] **feature 개선: idle 구간에 파묻히는 문제 수정** (2026-07-13).
  실기기 CSV(23,456샘플)로 처음 돌려보니 `accel_p90`이 거의 0으로 나왔다 — 실주행 세션
  대부분(90%+)이 idle(페달 안 밟음)이고 실제로 밟는 순간은 전체의 0.4~5%뿐이라, 세션 전체
  평균/percentile로는 "이 사람이 밟을 때 얼마나 깊게/자주 밟는지"를 전혀 못 잡는다.
  → `accel_active_mean/p90`(accel≥0.1인 샘플만 조건부 통계), `press_rate_per_min`(1분당
  페달 조작 횟수), `press_duration_mean_sec`(한 번 밟을 때 평균 지속시간) 추가.
  실기기 CSV 2개로 비교: `accel_active_mean`은 0.001(무의미) → 0.47(유의미)로 개선,
  `press_rate_per_min`이 두 세션 간 5.1회/분 vs 0.22회/분으로 뚜렷하게 갈림.
  - **다음 계획 (사용자 결정, 2026-07-13)**: 오조작 이벤트 라벨링(지도학습용)은 당분간
    개발자 전용 영역으로 남겨두고, 개발자 본인이 강함/보통/약함 3스타일로 직접 40~60초씩
    운전(조작)해 데이터를 모아 그 feature 차이로 프로필 판정 규칙을 도출하기로 함.
    → `DataCollectActivity`의 "오조작 구간 기록" 토글은 추후 "패턴 파악"(캘리브레이션)
    버튼으로 교체 예정: 사용자가 처음 앱을 켜고 누르면 40~50초 주행 패턴을 수집해
    자동으로 프로필을 판정 → 임계값 세트를 즉시 적용하는 흐름.
- [x] **1차 캘리브레이션 데이터 수집 및 feature 비교 분석** (2026-07-13).
  개발자 본인이 강/보통/약 3스타일로 각 90초 안팎 실주행 데이터 수집
  (`pedal_20260713_013502_strong.csv` 18,224샘플 / `..._013715_normal.csv` 19,280샘플 /
  `..._013902_weak.csv` 16,796샘플). `extract_features()`로 비교한 결과:

  | feature | strong | normal | weak |
  |---|---|---|---|
  | accel_p90 | 0.87 | 0.78 | 0.69 |
  | accel_active_p90 | 0.89 | 0.80 | 0.70 |
  | accel_active_mean | 0.62 | 0.62 | 0.51 |
  | press_rate_per_min | 22.4 | 5.0 | 5.7 |
  | press_duration_mean_sec | 1.8 | 9.2 | 8.1 |
  | brake_mean | 0.099 | 0.040 | 0.043 |

  - **`accel_p90`/`accel_active_p90`이 의도한 강>보통>약 순서로 가장 깔끔하게 갈림** →
    프로필 판정 규칙의 1순위 후보.
  - `press_rate_per_min`/`press_duration_mean_sec`은 "strong 대 나머지"는 확실히 가르지만
    normal과 weak는 거의 비슷(5.0 vs 5.7) — 구분력이 약함. accel_active_mean도 strong=normal이
    겹쳐서 약함.
  - ⚠️ **n=1(스타일당 세션 1개)이라 우연일 수 있음.** 컷오프 확정 전에 스타일당 2~3세트씩
    추가 수집해 재검증 필요(다음 작업으로 진행 중).
  - **별개 이슈로 발견**: 지금 `PROFILES`의 `accel_high`는 상황(시내 vs 고속도로 정속주행)과
    무관한 고정값이라, weak 프로필은 고속도로 정속 주행에서도 오탐 가능성이 있음(사용자 지적,
    2026-07-13). "이 사람이 어느 프로필인가"와 별개로 "지금이 어떤 주행 상황인가"도 필요할 수
    있음 — 후순위 과제로 남김.
- [x] **2차 캘리브레이션 데이터 수집 (스타일당 3세트, 총 9세션) 및 재검증** (2026-07-13).
  세션별 `accel_active_p90`:
  - weak: 0.483, 0.643, 0.699
  - normal: 0.725, 0.754, 0.804
  - strong: 0.800, 0.880, 0.888

  weak↔normal 경계(0.699 vs 0.725)는 3세트에서도 깔끔하게 유지됐지만, **normal↔strong 경계는
  거의 겹친다**(normal 최댓값 0.804가 strong 최솟값 0.800보다 오히려 높음) — 반복할수록 "강하게
  밟기"가 처음만큼 일관되지 않았거나, 두 스타일의 실제 조작 차이가 그렇게 크지 않았을 가능성.
  `press_rate_per_min`은 strong 대 나머지는 잘 갈리지만(10.9~22.4 vs 5~10) normal과 weak끼리는
  구분이 안 됨(범위가 거의 겹침) — 확정 채택 보류.
- [x] `judge.py`를 **프로필 인자를 받는 형태로 일반화** (2026-07-10).
      프로필 미지정 시 `normal` = 기존 휴리스틱. PC/앱 양쪽 동시 반영.
- [x] ~~프로필 → 임계값 세트 매핑 값 확정~~ — discrete 3단계 자체를 폐기(2026-07-13)해 해당 없음.
- [x] **프로필 판정 로직 1차 구현** (2026-07-13).
  `prototype/src/profile.py` 신규, `classify_profile(samples) -> "strong"/"normal"/"weak"`.
  `accel_active_p90` 컷오프(weak<0.71≤normal<0.80≤strong) 기반 — 9세션 실측에서 가장 안정적으로
  갈린 지표였다(모듈 docstring에 근거 기록). 9세션 교차검증 **8/9 정확도** — normal 최고
  세션(0.804)이 strong으로 오분류(위에서 언급한 경계 겹침과 동일 원인).
  ⚠️ 이 컷오프는 개발자 1인의 자기 라벨링 데이터 기반 임시값. normal↔strong 경계는 특히
  불안정하니 실사용자 데이터가 쌓이면 반드시 재조정. (실제 다수 사용자 데이터가 모이면
  경량 분류기로 교체 검토.)
- **⚠️ 설계 재검토 논의 (2026-07-13, 사용자 제기)**: 3단계 프로필 분류(2026-07-10 확정 방식)
  대신, **연속형 개인화**(예: `accel_high = 개인 평균 accel + 오프셋`)가 더 나을 수 있다는
  의견이 나옴 — 방금 겪은 "normal↔strong 경계가 애매해서 분류가 불안정한" 문제를 애초에
  버킷 경계가 없는 연속값 계산으로 피할 수 있어서다. 아직 **결론은 안 났고, 어느 쪽으로
  갈지 다음에 실험해보기로** 했다. 3단계 분류(`profile.py`)는 일단 그대로 두고 폐기하지 않음.
- [x] **CSV 로그 내보내기 단축 아이콘** (2026-07-13, 개발자용 반복 학습 편의).
  Phase E(증거자료용, 서명 필요)와는 성격이 다른 **가벼운 버전** — `DataCollectActivity`
  제목 옆에 저장 아이콘(`btnExportCsv`) 추가. 누르면 `filesDir/logs`의 CSV 전부를
  `cacheDir`에 zip으로 묶어 시스템 공유 시트(`ACTION_SEND`)로 내보낸다.
  `FileProvider`(`res/xml/file_paths.xml`, 매니페스트에 provider 등록) 신규 도입 —
  이전까지는 CSV 회수가 `adb`뿐이었는데, 이제 폰에서 바로 공유 가능.
  (매니페스트 등록 김에 `activity_data_collect.xml`에 XML 선언 앞에 실수로 들어가 있던
  글자 하나도 같이 제거 — 그대로였으면 빌드가 깨졌을 것.)
- [x] **연속형 개인화 1차 실험 — 위 "설계 재검토"의 후속** (2026-07-13).
  9세션(스타일당 3개) 각각에 대해 `accel_high = min(baseline + offset, 1.0)`을 계산하고,
  그 세션 **자기 자신**(전부 정상 주행)에 대해 0.25초 tumbling 윈도우(`brake_low=0.10`,
  `high_ratio=0.50` 고정)로 판정했을 때 오탐률이 얼마나 나오는지 측정.

  | baseline | offset | 9세션 중 최악 오탐률 |
  |---|---|---|
  | accel_active_mean | +0.10 | 34.3% |
  | accel_active_mean | +0.20 | 22.5% |
  | accel_active_mean | +0.30 | 2.5% |
  | accel_active_p90  | +0.02 | 5.2% |
  | accel_active_p90  | +0.05 | 3.3% |
  | accel_active_p90  | +0.08 | 2.0% |
  | accel_active_p90  | **+0.10** | **1.5%** |
  | accel_active_p90  | +0.15 | 1.0% |

  **`accel_active_p90` 기준이 `accel_active_mean`보다 훨씬 효율적** — mean은 변동성 큰
  세션(`strong_1`, accel_std=0.363)에 잘 안 먹혀서 +0.30까지 줘야 안정되는데, p90은
  이미 그 사람의 튀는 값을 반영하고 있어서 +0.08~0.10만으로 전 세션 2% 이하로 내려감.
  **잠정 공식**: `accel_high = min(accel_active_p90 + 0.10, 1.0)`.

  ⚠️ **한계**: 이건 "정상 주행에서 오탐이 적은가"만 검증했다. 9세션 전부 misop 라벨이
  없어서 **실제 오조작을 놓치지 않고 잡아내는지(민감도)는 미검증** — 오조작 이벤트
  라벨링 데이터가 있어야 확인 가능(§6 "오조작 이벤트 라벨링은 개발자 전용" 항목 참고).
  brake_low/high_ratio 개인화는 아직 안 함(고정값으로 실험).
  → 3단계 분류(discrete) vs 연속형 공식 중 **최종 채택은 아직 결정 전**.
- [x] **연속형 개인화 1차 적용 (PC 프로토타입)** (2026-07-13).
  `judge.py` 리팩터링: 판정 코어를 `judge_with_thresholds(samples, thresholds, label)`로
  분리하고, 기존 `judge(samples, profile)`/`judge_json`은 그 위에 얇게 래핑 — **기존
  호출부(Kotlin 포함) 시그니처는 그대로 유지**돼 하위 호환 깨지지 않음.
  `judge_calibrated_json(samples_json, thresholds_json)` 신규 — 3단계 프로필 대신
  캘리브레이션으로 계산한 연속형 임계값을 JSON 경계로 받는 경로(향후 Kotlin 연동용).

  `prototype/src/calibration.py` 신규: `calibrate_thresholds(samples) -> {"accel_high":..,
  "brake_low":.., "high_ratio":..}`. 위 실험에서 나온 `accel_active_p90 + 0.10` 공식을
  그대로 구현. brake_low/high_ratio는 아직 `PROFILES["normal"]`과 동일한 고정값.

  9세션에 실제 함수 경로(`calibrate_thresholds` → `judge_with_thresholds`)로 재검증 —
  실험과 동일하게 전 세션 오탐률 1.5% 이하 재현. 세션별 계산된 `accel_high`가 0.58(약함
  세션)~0.99(강함 세션)로 개인차가 자연스럽게 반영됨을 확인.

  ⚠️ 아직 **PC 프로토타입에서만** 검증됐다. 앱(Kotlin/BleService) 연동은 별개 작업 —
  "패턴 파악" 캘리브레이션 버튼(§4 Phase B 앞부분에서 논의)에서 이 경로를 호출하도록
  나중에 연결. discrete 3단계(`profile.py`)와 연속형(`calibration.py`) 중 최종 채택은
  여전히 미정 — 둘 다 코드로는 존재하며 병행 실험 가능한 상태로 남겨둠.
- [x] **연속형 개인화, 앱(BleService) 연동 + 실기기 검증** (2026-07-13).
  `app/src/main/python/`에 `judge.py`(리팩터링 버전으로 동기화) + `features.py` +
  `calibration.py` 복사 — 프로토타입과 바이트 단위 동일 유지.

  `BleService`: `ACTION_START_CALIBRATION` → 45초간 원시 샘플 버퍼링 → 종료 시
  `calibration.calibrate_thresholds_json` 호출 → 결과를 `SharedPreferences`에 저장 +
  `ACTION_CALIBRATION_DONE` 브로드캐스트. 세션 시작(연결) 시점에 이전 캘리브레이션 값을
  prefs에서 복원해 재사용(Phase C "매번 재분류 X"를 이 형태로 선행 구현).

  `DataCollectActivity`: "패턴 파악 시작 (45초)" 버튼 추가 — 카운트다운 표시, 완료되면
  그래프 임계선 자동 갱신 + 토스트로 계산된 `accel_high` 표시. **실기기에서 정상 동작
  확인됨.**

  - **⚠️ 판정 정책 변경 (사용자 지적, 2026-07-13)**: 처음엔 캘리브레이션 없을 때 3단계
    프로필로 폴백했는데, 이러면 "패턴 파악" 도중 의도적으로 세게 밟는 순간에도 오조작
    경고(알림/음성/`CriticalActivity`)가 떠서 캘리브레이션 데이터 수집이 불편했다.
    → **"임계값 미설정 = 판정 안 함"으로 정책 변경**. `judgeWindow()`가 캘리브레이션
    중이거나 `calibratedThresholdsJson`이 없으면 판정 자체를 건너뛴다(3단계 프로필
    폴백 완전 제거). `startCalibration()`은 시작하는 순간 기존 캘리브레이션 값을 즉시
    `null`로 되돌려 "미설정" 상태로 만든다 — 재캘리브레이션 시작 시 이전 값이 판정에
    끼어들지 않는다.
    ⚠️ **트레이드오프**: 이제 사용자가 한 번도 캘리브레이션하지 않으면 오조작 감지가
    아예 동작하지 않는다(이전엔 최소한 `normal` 프로필로 동작했음). "패턴 파악을 먼저
    하도록 유도하는 온보딩"이 사실상 필수가 됐다는 뜻 — Phase C에서 신경 쓸 것.
- [x] **`ACCEL_HIGH_OFFSET` 재조정: 0.10 → 0.20** (2026-07-13, 실기기 캘리브레이션 후 피드백).
  accel 0.6~0.7 유지하며 캘리브레이션했더니 `accel_high=0.84`로 잡혔는데, "평소 밟는
  범위"와 "브레이크로 착각해 콱 밟는 슬램"이 값으로 거의 안 갈렸다. 오탐 방지 실험
  (§Phase B 위쪽)은 "정상 주행에서 오경보 안 나는가"만 봤지 "평소 범위와 슬램 사이에
  체감할 만한 여유가 있는가"는 검증한 적이 없었다.
  → 0.20으로 올리면 위 예시가 `accel_high≈0.94`가 돼서 여유가 커짐. **아직 실기기
  재검증 전** — 다음 테스트에서 이 정도 여유가 체감상 적절한지 확인 필요.
- [x] **discrete 3단계 프로필 최종 폐기 + 캘리브레이션 강제 온보딩** (2026-07-13, 사용자 결정).
  연속형이 잘 동작하는 걸 확인한 뒤 "3단계 분류는 이제 없애도 될 것 같다"고 확정.
  - `prototype/src/profile.py`(classify_profile) 삭제 — 앱에 연결된 적도 없어 안전하게 제거.
  - `DataCollectActivity`의 강함/보통/약함 토글(`toggleProfile` 및 관련 버튼) 제거,
    `activity_data_collect.xml`에서도 해당 뷰 삭제.
  - `BleService`의 `sessionProfile`/`PREF_PROFILE`/`DEFAULT_PROFILE`/`VALID_PROFILES`/
    `readProfile()` 전부 제거(더는 아무도 쓰지 않는 죽은 코드였음). CSV 파일명도
    `pedal_yyyyMMdd_HHmmss_<profile>.csv` → `pedal_yyyyMMdd_HHmmss.csv`로 단순화.
  - `judge.py`/`PROFILES`/`judge()`/`judge_json()`/`thresholds_json()`은 PC 프로토타입
    (`run.py` 데모, 오프라인 실험용)에선 계속 쓰이므로 **prototype/앱 양쪽 다 그대로 유지**
    (동기화 대상 파일 자체를 건드리지 않음) — 단지 앱의 실시간 판정 경로에서만 안 쓰임.
  - **캘리브레이션 강제 온보딩**: `MainActivity`가 `ACTION_UUID_MATCHED`(연결 성공) 수신 시
    `PREF_CALIBRATED_THRESHOLDS`가 없으면 팝업을 띄우고, 버튼을 누르면 바로
    `DataCollectActivity`로 이동시켜 캘리브레이션을 유도한다. 캘리브레이션할 때까지
    연결할 때마다 다시 뜬다(같은 팝업 중복 표시는 플래그로 방지).
  - 이걸로 §Phase C의 "discrete 3단계 vs 연속형 최종 채택 미정" 항목과 "캘리브레이션
    안 하면 감지 안 됨 → 온보딩 필요" 트레이드오프가 둘 다 해소됨.
- [x] **캘리브레이션 초기화 버튼** (2026-07-13). "삭제 후 재설치한 것처럼" 되돌리고 싶다는
  요청 — `DataCollectActivity`의 "패턴 파악" 버튼 옆에 작은 초기화 아이콘(`btnResetCalibration`)
  추가. `BleService`에 `ACTION_CLEAR_CALIBRATION` 추가: `calibratedThresholdsJson`을 `null`로
  되돌리고 prefs에서도 제거 — 이후 `judgeWindow`가 다시 판정을 건너뛰는(오조작 감지 꺼짐)
  "캘리브레이션 전" 상태로 복귀한다. 안전 관련 동작이라 초기화 전 확인 다이얼로그를 거친다.
  그래프는 `PedalGraphView.DEFAULT_ACCEL_HIGH/DEFAULT_BRAKE_LOW`(0.85/0.10, 참고용 상수로
  분리) 참고선으로 되돌아간다.
- [x] **실주행 CSV 오프라인 검증 파이프라인** (2026-07-18). `prototype/src/analyze_field_data.py`
  신규 — `수집 데이터/` 폴더의 CSV를 읽어 (1) style별 `extract_features()` 비교, (2) 세션별
  `calibrate_thresholds()` 재계산으로 안정성 확인, (3) 오조작 `label`(정답) vs `pedal_err`
  (실시간 판정) 매칭으로 recall/오탐 확인을 자동으로 리포트한다. 인자 없이 실행하면
  `수집 데이터/`를 기본 스캔.
  - **style 구분**: 2026-07-18 세션 3개(각 세션에 weak/normal/strong 다 포함) 전부에서
    `accel_active_p90` 순서(weak<normal<strong)가 깨지지 않음 확인 — 7/13 실험 때 n=1이라
    우연일 수 있다던 우려가 반복 검증으로 완화됨. 다만 절대값은 세션마다 여전히 흔들림(연속형
    캘리브레이션 채택이 맞는 방향임을 재확인).
  - **캘리브레이션 오염 문제 발견**: 강/보통/약이 섞인 세션을 통째로 캘리브레이션 입력이라
    가정하고 계산하면 `accel_high`가 0.90~0.99로 천장에 붙음. 실제로 **정상 주행만으로 30초
    캘리브레이션**(`30s_cal_data.csv`)한 결과는 `0.6577`로 훨씬 현실적 — 캘리브레이션 세션에
    강하게 밟는 구간이 섞이면 임계값이 과대해짐을 실측으로 확인. 온보딩 안내("이 30초 동안은
    평소처럼만 주행")의 중요성 근거.
  - **trigger(급조작) 설계 검증**: `확_스냅_구분_데이터.csv`에서 "천천히 확실하게 깊게 밟기"
    (2.5초간 accel 1.0 유지)가 trigger 미발동으로 경고 안 뜨는 사례 관측 → 사용자가 **의도한
    설계**라고 확정(§5 핵심 의사결정 "`accel_high` 개인화가 정말 필요한가" 항목 참고). 고속도로
    합류·차선변경처럼 서서히 엑셀을 깊게 밟는 정상 주행을 오조작으로 안 잡기 위해 일부러 넣은
    조건이라는 것.

### Phase C — 폰 온디바이스 추론
- [x] Chaquopy로 추론 — `calibration.py`/`judge.py` 자체가 순수 Python이라 별도 번들
  없이 이미 동작 중(연속형 개인화 경로 한정. numpy/sklearn 은 필요해진 적 없음).
- [x] 주행 초기 N초(현재 30초, 조정 가능) 데이터로 임계값을 캘리브레이션 → 즉시 적용
  ("패턴 파악" 버튼, 2026-07-13 앱 연동 완료).
- [x] 캘리브레이션 결과를 저장해 다음 주행에 재사용(매번 재캘리브레이션 X) — prefs 복원 완료.
- [x] ~~"학습된 분류기 아티팩트 번들"~~ — 3단계 discrete 분류(`profile.py`)를 폐기하고
  연속형(`calibration.py`)으로 최종 확정(2026-07-13)해 해당 없음으로 종료.

### Phase E — 로깅 데이터 추출 / 증거자료화
> Phase B·C(개인화)가 끝난 뒤 착수하기로 했던 항목(2026-07-10) — 2026-07-13 착수, 2026-07-14
> 서명(무결성)까지 구현 완료. 핵심 기능은 다 됐고 남은 건 CSV 스키마 추가 확장 여부 정도.

**배경**: 기존 하드웨어 구조에서는 모듈의 SD 카드에 페달 입력 수치·주행상황(정상/경고)·날짜·시각을
CSV로 로깅해, 실제 급발진 사고 발생 시 사용자가 직접 열어 **증거자료**로 쓸 수 있게 했다.
현 구조에서는 SD 카드가 빠지고 폰이 로깅을 맡으므로(`filesDir/logs/*.csv`), 그 기능을 앱에서 되살린다.

- [x] **판정 이벤트 로그 분리 시도 → 곧바로 단일 파일 병합으로 재수정 + 내보내기 UI** (2026-07-13,
  서명 없는 평문 버전).
  - 1차로 `events_*.csv`(4Hz 판정 전용, 별도 파일)를 만들었으나, 실기기 테스트 직후 사용자가
    "날짜/시간/brake/accel/module_err/misop/임계값 초과량을 한 파일로 달라"고 요청 → **병합
    방식으로 재수정**. `events_*.csv` 관련 코드 전부 제거하고 `pedal_*.csv`(원시 CSV) 스키마를
    확장하는 쪽으로 바꿨다.
  - **최종 스키마**: `pedal_yyyyMMdd_HHmmss.csv` = `date,time,brake,accel,module_err,pedal_err,accel_exceed,label`.
    - `date`/`time`: 기존 `recv_epoch_ms`(원시 epoch) 대신 사람이 읽을 수 있는 형식.
    - `module_err`: 그 순간 미해결 장치 오류 여부 — `"module_err"`/`"none"` 문자열(기존
      `isErrorDialogShowing` 재사용).
    - `pedal_err`: 그 판정 윈도우의 오조작 여부 — `"pedal_err"`/`"none"` 문자열(judge.py의 misop과
      동일 개념, 컬럼명만 사용자 요청으로 통일). accel이 임계값을 넘고 brake는 거의 안 밟힌
      상태로 잡혔다는 뜻.
    - `accel_exceed`: 대표 accel 값이 그 순간 캘리브레이션된 `accel_high`를 넘은 양(안 넘었거나
      캘리브레이션 전이면 0).
    - `label`: 기존 수동 라벨링(개발자용 지도학습 정답, Phase A)은 그대로 유지.
    - `cnt`/`recv_epoch_ms`는 스키마에서 제거(내부 누락 검출용 `lastCnt`는 코드에 남아있음).
    - ⚠️ 처음엔 `module_err`/`misop`을 0/1로 남겼다가, "열어봤을 때 바로 알아볼 수 있게"
      문자열(`module_err`/`pedal_err`/`none`)로 바꾸고 `misop` 컬럼명도 `pedal_err`로 변경
      (2026-07-13, 실기기 테스트 후 피드백).
  - **⚠️ 기록 주기 재조정: 200Hz(샘플당) → 4Hz(판정 윈도우당 1행)** (2026-07-13, 실기기 테스트 직후).
    처음엔 원시 샘플 200Hz 전부를 이 스키마로 기록했더니 시간당 수십만 행이 나와 "초당 데이터가
    너무 많다"는 피드백. `misop`/`accel_exceed`가 애초에 4Hz 판정 단위라 200Hz로 찍어도 같은 값이
    반복될 뿐이라, 판정 윈도우(50샘플=0.25초)당 1행만 기록하도록 변경 — `brake`/`accel`은 그
    윈도우의 마지막 샘플값을 대표값으로 씀. 캘리브레이션 정확도는 영향 없음(캘리브레이션은
    CSV가 아니라 메모리 버퍼에서 200Hz 그대로 계산). "증거자료는 사람이 열어볼 수 있는 크기여야
    한다"는 원칙에 따른 결정.
  - 구현: `calibratedThresholdsJson`을 세팅할 때마다 `accel_high`를 같이 파싱해 캐시하는
    `applyCalibratedThresholds()`/`currentAccelHigh` 추가 — 200Hz마다 JSON 파싱하는 낭비를 피함.
  - `activity_device_manager.xml`에 `btnExportLogs` 아이콘 추가(`btnOpenSettings` 옆).
    탭 → `pedal_*.csv` 파일명에서 세션 타임스탬프 목록 추출 → **"26/07/13-03:28:47"처럼 사람이
    보기 좋은 형식으로 변환**(`formatSessionLabel()`)해 `AlertDialog.setItems()`로 세션 선택.
    `FileProvider`에 `filesDir/logs`용 `files-path` 추가(기존엔 `cacheDir`만 노출돼 있었음).
  - ⚠️ 처음엔 서명 없이 CSV 파일 하나만 직접 공유(`text/csv`)했으나, 바로 아래 서명 기능이
    들어오면서 다시 **zip 방식으로 변경**(CSV + `.sig` + `pubkey.pem` 세 파일을 묶어야 하므로).
- [x] **무결성 보장 구현: 평문 CSV + Android Keystore 분리 서명** (2026-07-14).
  유력안 그대로 구현 완료 — 공개키는 내보내는 묶음(zip)에 같이 포함, PC 검증은 별도 스크립트.
  - `app/.../EvidenceSigner.kt` 신규(object): `AndroidKeyStore`에 EC(`secp256r1`) 키 쌍을
    최초 서명 시점에 lazy 생성(`peob_evidence_signing_key` 별칭) — 개인키는 하드웨어 보안
    저장소 밖으로 절대 안 나감(Keystore가 서명 연산만 대행, `getKey()`로 반환되는 건
    `PrivateKey` 핸들일 뿐 원본 키 바이트 접근 불가).
    - `signFile(file)`: `SHA256withECDSA`로 파일 전체 서명 → base64 문자열 반환.
    - `publicKeyPem()`: Keystore 인증서에서 공개키를 X.509 PEM 형식으로 추출.
  - `DeviceManagerActivity.exportSession()`: CSV 서명 → `pedal_*.csv` + `pedal_*.csv.sig`
    (base64 텍스트) + `pubkey.pem` 세 파일을 zip으로 묶어 공유 시트로 내보냄. 서명 실패해도
    (예: Keystore 오류) CSV 자체는 서명 없이 내보내지도록 예외 처리(안전한 폴백).
  - **PC 검증 스크립트**: `prototype/verify_signature.py` 신규 (최종적으로 stdlib만 사용 —
    아래 "cryptography 의존성 완전 제거" 항목 참고).
    `uv run python verify_signature.py pedal_<ts>.csv` (같은 폴더의 `.sig`/`pubkey.pem` 자동 탐색,
    `--sig`/`--pubkey`로 경로 직접 지정도 가능). ECDSA-SHA256 검증, VALID/INVALID 출력 +
    종료 코드(0=정상, 1=변조, 2=파일/오류). 합성 키로 정상 케이스(VALID)와 변조 케이스
    (1바이트 추가 후 INVALID) 둘 다 실기기 없이 로컬 검증 완료.
  - **✅ 실기기 검증 완료(2026-07-14)**: 실제 폰에서 내보낸 세션을 PC에서 `verify_signature.py`로
    검증 — 원본 그대로면 VALID, 다운로드한 CSV를 스프레드시트 앱으로 열어 저장(대문자 자동보정
    + 끝 개행 소실 발생)한 뒤 재검증하니 정확히 INVALID로 잡힘. 의도한 대로 동작 확인.
  - **검증 스크립트를 zip 안에 동봉** (2026-07-14, 사용자 피드백). 처음엔 "PC에서 `uv run`으로
    실행하라"고 안내했는데, 검증은 이 저장소/uv가 없는 **제3자(경찰/보험사) 컴퓨터**에서 하는
    거라 그 전제가 의미 없다는 지적 — 개인 zsh alias 등록도 마찬가지로 무의미하다고 판단.
    → `verify_signature.py`를 `app/src/main/assets/verify_signature.py`로도 복사해두고
    (`prototype/verify_signature.py`와 동일 내용 유지 — judge.py 등과 같은 동기화 원칙),
    `exportSession()`이 zip에 CSV/.sig/pubkey.pem과 함께 이 스크립트도 넣는다.
  - **`cryptography` 의존성 완전 제거 → 표준 라이브러리만으로 ECDSA 재구현** (2026-07-14,
    같은 자리에서 이어진 피드백). "제3자 컴퓨터에 `pip install`조차 안 될 수 있다"는 지적에
    따라, `verify_signature.py`의 ECDSA(P-256/SHA-256) 서명 검증을 순수 Python으로 직접
    구현(DER 파싱 + 타원곡선 점 덧셈/배가 + `pow(x, -1, p)` 모듈러 역원, hashlib.sha256만
    사용). `cryptography`로 만든 서명을 이 구현으로 교차 검증(정상 VALID / 변조 INVALID)
    통과 — Android Keystore도 동일 표준(X.509 DER/PEM)을 쓰므로 실기기 서명과도 호환.
    `prototype/pyproject.toml`의 `cryptography` 의존성 제거(`uv remove`), 다시 완전
    무의존성(stdlib only) 상태로 복귀. 이제 받는 쪽은 `python3 verify_signature.py <csv>`
    만으로 검증 가능 — pip install도, 인터넷 연결도, 저장소 클론도 전혀 필요 없다.
  - **비개발자용 사용법 안내문 동봉** (2026-07-14, 사용자 요청 — "컴퓨터 잘 모르는 사람도
    쓸 수 있게"). `prototype/README_검증방법.txt` 신규(→ `app/src/main/assets/`에도 동일하게
    복사) — 터미널/커맨드라인 개념을 모르는 사람 기준으로 Windows(PowerShell 열기,
    Python 설치 시 "Add to PATH" 체크 안내)와 Mac(터미널 앱 찾기, Finder에서 폴더를
    드래그해 경로 자동 입력하는 법)을 단계별로 설명. `exportSession()`의 zip에 5번째
    파일로 포함(CSV/.sig/pubkey.pem/verify_signature.py/README_검증방법.txt).
  - ⚠️ **아직 안 한 것**: 공개키 자체의 진정성 증명(예: 폰 최초 등록 시 공개키를 신뢰 가능한
    별도 채널로 공지하는 것)은 범위 밖 — 지금은 "같은 zip 안에 공개키 포함"까지만. 검증자가
    "이 공개키가 정말 이 특정 폰 것"이라는 건 별도로 신뢰해야 한다(사용자 결정, 2026-07-14).
- [x] **주행 기록 삭제 기능** (2026-07-14, 사용자 요청 — "재설치 말고 앱 안에서 지우게").
  `filesDir/logs`는 세션마다 계속 쌓이기만 하고 자동으로 안 지워지는 문제. 설정 화면
  헤더에 `btnDeleteLogs`(휴지통 아이콘) 추가 → 세션 목록 다이얼로그(맨 위에 "전체 삭제
  (N개 세션)" 옵션 + 개별 세션 목록, `btnExportLogs`의 세션 선택 UI와 동일한 형식 재사용)
  → 선택 시 확인 다이얼로그(되돌릴 수 없음 경고) → 확인해야 실제 삭제.
  ⚠️ 지금 감시 중인 세션(아직 기록 중인 CSV)을 삭제해도 앱이 죽진 않지만(파일이 지워진
  상태로 계속 쓰기만 함) 이후 그 세션 기록은 사라진다 — 현재 세션을 목록에서 구분해
  막는 건 안 함(1차 구현 범위 밖, 필요해지면 추가).

> ⚠️ **"암호화"와 "증거자료"는 요구가 다르다.** 최초 아이디어는 '암호화된 CSV 추출'이었으나:
> - 암호화 = **기밀성**(남이 못 봄). 증거에 필요한 건 **무결성·진정성**("사고 당시 그 기기에서 나왔고, 이후 변조 없음").
> - 사용자가 복호화할 수 있으면 **수정 후 재암호화도 가능** → 암호화만으로는 변조를 못 막는다.
> - 경찰/보험사/법원이 **열 수 없는 파일은 증거 가치가 떨어진다.**
>
> → 유력안: **평문 CSV + 분리 서명(`.sig`)**. Android Keystore의 하드웨어 백업 개인키(추출 불가)로
>   CSV 해시에 서명하고 공개키를 공개. 누구나 열람 가능하되 1바이트만 바뀌어도 검증 실패.
>   (블랙박스·EDR이 쓰는 방식) 기밀성이 필요하면 **저장이 아니라 '공유 시점'** 에 ZIP 암호 등으로 처리.


### Phase D — 개인화 적응 (후순위)
> 2026-07-13부로 개인화 방식이 **연속형 캘리브레이션**(`calibration.py`)으로 확정됐다.
> 아래는 그 위에 얹을 수 있는 추가 적응 아이디어 — 아직 착수 전.
- [ ] 주행 데이터가 누적되면 캘리브레이션 재실행을 유도(습관이 바뀌었을 수 있음).
- [ ] (선택) 세션이 쌓일수록 `accel_active_p90`을 점진 갱신해 오경보 추가 감소.
- [ ] (선택) 여러 사용자 데이터를 모아 **공통 모델 + 개인 보정**(federated/transfer) 구조로 확장.
- [x] **로지스틱 회귀 1차 실험** (2026-07-18, `prototype/src/train_logreg.py` 신규).
  sklearn/numpy 없이 경사하강법 직접 구현(§확정된 "무의존성" 기조 유지 — 학습된 가중치를
  그대로 Chaquopy에 옮길 수 있어야 함). feature: `accel`/`brake`/`accel_delta_1`(0.25초
  델타)/`accel_delta_4`(1초 델타). 라벨 있는 세션 2개로 leave-one-session-out 검증(무작위
  행 분할은 같은 이벤트의 연속 행이 학습/검증에 동시에 섞이는 leakage가 생겨서 세션째로 분리).
  - **결과**: recall 54~68%는 봐줄 만한데 **precision 18~30%로 오탐이 많음** — 지금 규칙
    기반(`judge.py`, sustain+trigger)이 오탐 0건이었던 것보다 명백히 나쁨. **아직 judge.py를
    대체할 수준이 전혀 아니다.**
  - **원인 오판 → 정정 (같은 날)**: 처음엔 `brake` 가중치가 거의 0인 걸 보고 "브레이크
    동시 조작 대조군이 없어서"라고 판단했었는데, 사용자가 정정: **실주행에서 엑셀·브레이크
    동시 조작은 애초에 안 일어나므로 그런 대조군은 비현실적**. 진짜 원인은 학습 데이터가
    전부 "오조작 재현" 세션뿐이라, style 세션(수집 데이터 (1)/(2)/(3).csv, strong 스타일의
    정상적으로 강한 가속 포함)을 모델이 한 번도 label=0으로 못 본 것 — negative 예시 부족.
  - **2차 실험**: `load_session()`을 고쳐 label 컬럼 없는 세션(style/캘리브레이션, 오조작
    재현 자체를 안 한 세션)을 전부 label=0으로 포함시켜 재학습(총 6801행, 그중 label=1은
    182행뿐). **결과가 오히려 더 나빠짐** — style 세션을 검증용으로 뺐을 때 오탐이
    300~400건씩 쏟아짐(예: `수집 데이터 (2).csv` 2232행 중 404건 오탐). 라벨 세션 precision도
    소폭 하락(0.30→0.24, 0.18→0.17).
  - **결론(중요)**: negative 예시를 늘려도 안 나아진다는 건, **지금 쓰는 4개 feature(accel,
    brake, 0.25초/1초 델타)만으로는 "강하게 밟는 정상 주행"과 "오조작"이 애초에 선형으로 안
    갈린다**는 뜻 — 둘 다 accel이 1.0까지 올라가는 건 같고, 실제로 구분되는 지점은 §5
    핵심 의사결정에서 이미 확정한 대로 **"trigger로 무장된 뒤 얼마나 오래 지속되는가"**(시간
    축 정보)인데 지금 로지스틱 회귀는 행 단위 독립 판정이라 이 정보를 아예 안 씀. 이 시간축
    feature를 추가하면 사실상 judge.py의 sustain/trigger 로직을 다시 학습으로 베끼는 셈이라
    지금 데이터량으로는 실익이 낮음. → **로지스틱 회귀는 여기서 보류**, 규칙 기반
    (`judge.py`)의 `accel_rate_high` 실측 튜닝에 우선순위를 둔다(사용자 결정, 2026-07-18).
    파이프라인(`train_logreg.py`) 자체는 재사용 가능하니 데이터/feature가 더 갖춰지면 재개.

### 추가 패치 내역 - retouch of pedal_error logic — **구현 완료 (2026-07-14)**
- **기존**: 단순히 임계값 넘어서 N window 이상 지속 시 pedal_err 트리거 작동 (레벨 조건만 사용 —
  확 밟은 것과 서서히 깊게 밟아가는 정상 주행을 구분 못 함)
- **수정**: `judge_with_thresholds()`가 레벨 조건(`misop`)과 별개로 미분 조건(`trigger`, 연속
  샘플 간 엑셀 델타의 최댓값 ≥ `accel_rate_high`)을 함께 반환. `BleService`는 `trigger`가 한 번
  나오면 `armed=true`로 무장하고, **무장된 동안에만** 기존 `misop` 지속시간 카운트를 진행한다.
  트리거는 순간적 사건이라 그 자체를 여러 윈도우 연속으로 요구하면 스파이크 직후 델타가 다시
  작아져 무너지므로, "지속"은 여전히 레벨(misop) 쪽에서 담당하고 트리거는 무장만 담당하도록
  역할을 분리했다 (§5 핵심 의사결정 참고).



---

## 5. 핵심 의사결정 메모

### 확정됨 ✅
- **개인화 방식 (2026-07-10 확정 → 2026-07-13 연속형으로 재확정)**: 처음엔 3단계 프로필
  (강/보통/약) discrete 분류로 갔었는데, 실측에서 normal↔strong 경계가 자꾸 겹쳐 불안정
  했음(§Phase B). → **캘리브레이션 세션(현재 30초)에서 뽑은 `accel_active_p90 + 오프셋`으로
  개인별 `accel_high`를 직접 계산하는 연속형 방식**으로 최종 전환. discrete 분류(`profile.py`)는
  폐기·삭제. `judge.py`의 `PROFILES`/3단계 상수는 PC 프로토타입 데모(`run.py`)용으로만 남음.
- **모델 종류**: 학습된 분류기가 아니라 **통계 규칙 기반 캘리브레이션**(percentile + 고정 오프셋).
  여러 사용자 데이터를 모아 분류기를 학습하는 방향은 채택 안 함(현재로선 불필요 — 후순위 Phase D).
- **학습/추론 위치**: 캘리브레이션 계산도 추론도 전부 폰(Chaquopy, 순수 Python) — PC(`prototype/`)는
  같은 로직을 미리 검증해보는 용도.
- **로그 추출 보안 모델**: **평문 CSV + 분리 서명(.sig)** 채택 및 구현 완료(2026-07-14).
  Android Keystore 하드웨어 EC 키로 서명, 공개키는 내보내는 zip에 동봉. 검증은 PC 스크립트
  (`prototype/verify_signature.py`, 표준 라이브러리만으로 ECDSA 직접 구현 — 제3자 컴퓨터에
  pip install도 필요 없게). 서명 키는 최초 서명 시점에 lazy 생성.
- **이벤트 로그 분리 여부**: 처음엔 `events_*.csv`로 분리했다가, "한 파일로 달라"는 피드백으로
  **`pedal_*.csv` 하나에 병합**하는 쪽으로 확정(날짜/시간/brake/accel/module_err/pedal_err/
  accel_exceed/label 컬럼, 판정 윈도우당 1행=4Hz).
- **오조작 경고 트리거 방식 (2026-07-13 확정)**: 기존엔 판정 윈도우(0.25초) 1개가 임계값을
  넘으면 즉시 경고했는데, 급발진은 순간 스파이크가 아니라 "꾹 밟고 유지"하는 패턴이라 짧은
  오조작(밟았다 바로 뗌)과 구분이 안 됐음. → `BleService.judgeWindow()`에 **연속 윈도우
  카운터**를 추가해 `misop=true`가 `MISOP_SUSTAIN_WINDOWS`(임시 8윈도우=2.0초) 이상 연속
  이어져야 경고하도록 변경. 카운트 도중 짧게 `misop=false`가 섞여도(`MISOP_GAP_TOLERANCE_WINDOWS`,
  임시 2윈도우=0.5초 이내) 바로 리셋하지 않고 봐주는 여유를 둠. `judge.py`는 순수 함수 그대로
  유지하고 지속시간 로직은 전부 Kotlin(`BleService`) 쪽에서 관리.
- **급조작 vs 서서히 증가 구분 — 트리거/지속 역할 분리 (2026-07-14 확정)**: 위 지속시간 로직은
  "확 밟고 유지"와 "천천히 깊게 밟아가며 유지"를 구분 못 하는 한계가 있었음(둘 다 레벨 조건만
  봄). → `judge.py`에 미분 기반 `trigger`(연속 샘플 간 엑셀 델타 최댓값 ≥ `accel_rate_high`)를
  추가하고, `BleService`에서 "트리거가 무장(arm) → 무장된 동안에만 레벨 지속시간 카운트 진행"
  구조로 결합. 트리거 자체는 순간적 사건이라 지속시간 요구 대상에서 제외(요구하면 스파이크
  직후 델타가 사라져 카운트가 안 이어짐) — 그래서 "트리거=한 번의 무장 조건", "지속=레벨 조건"
  으로 역할을 나눔. `PROFILES`/`calibration.calibrate_thresholds()` 양쪽에 `accel_rate_high` 필드 추가.
- **캘리브레이션 중 다른 앱으로 전환해도 유지 (2026-07-14 확정)**: 사용자가 "패턴 파악(30초)"
  버튼을 누른 뒤 내비 앱 등으로 전환하면 캘리브레이션이 멈추는 것처럼 보인다는 문제 제기.
  → 실제로는 `isCalibrating`/`calibrationBuffer`/`finishCalibration()`이 전부 `BleService`
  (Activity와 무관한 포그라운드 서비스) 안에서만 돌아서 **원래도 백그라운드에서 안 멈추고
  계속 진행되고 있었음** — 진짜 버그는 UI 쪽: `DataCollectActivity`가 `onResume()`에서만
  `ACTION_CALIBRATION_DONE`을 구독해서, 화면이 꺼져 있는 동안 캘리브레이션이 끝나면 완료
  브로드캐스트가 유실되고, `showExistingCalibration()`도 `onCreate()`에서만 호출돼 재개
  (resume)만 됐을 땐 최신 임계값을 다시 안 읽어와 그래프가 안 바뀐 것처럼 보였음(재진입
  테스트로 확인 — 뒤로 갔다 다시 들어가면 그래프가 바뀌어 있었음). → `BleService`에
  `PREF_CALIBRATION_START_MS`(캘리브레이션 시작 시각)를 prefs에 남기고, `DataCollectActivity`는
  **매 `onResume()`마다** `showExistingCalibration()`으로 그래프를 최신화하고, 아직 진행
  중이면 남은 시간을 역산해 카운트다운 UI도 복구하도록 수정. 추가로 `finishCalibration()`
  성공 시 `showHeadsUpNotification()`으로 "패턴 파악이 완료되었습니다" 시스템 알림을 띄워
  화면(Activity) 재진입 없이도 완료 사실을 바로 알 수 있게 함.

### 아직 정해야 할 것
- **Chaquopy 패키지 제약**: 지금은 numpy/sklearn 없이 순수 Python(judge/features/calibration)만
  써서 이 문제 자체가 사라짐 — 재검토 불필요(연속형 캘리브레이션 방식 채택 결과).
- **프라이버시**: 페달 데이터는 개인 운전 습관 → 로컬 저장 원칙, 외부 전송 시 동의/익명화.
  아직 명시적 동의 UI는 없음(캘리브레이션/로그 저장이 전부 로컬이라 급하진 않음).
- **공개키 자체의 진정성 증명**: 지금은 "같은 zip 안에 공개키 포함"까지만 — "이 공개키가
  정말 이 특정 폰 것"이라는 건 검증자가 별도로 신뢰해야 함(§Phase E 참고, 범위 밖으로 명시).
- **캘리브레이션 오프셋(`ACCEL_HIGH_OFFSET=0.20`)이 여러 사용자·다양한 실제 주행 상황(고속도로
  정속 주행 등)에서도 오탐/누락 없이 적절한지**는 아직 소규모(개발자 1인) 테스트 수준.
- ~~`accel_high`(레벨 임계값) 개인화가 정말 필요한가?~~ — **결론 남(2026-07-18, 사용자 확정)**.
  2026-07-14에 제기됐던 문제 제기: 공황 상태로 브레이크인 줄 알고 엑셀을 콱 밟는 오조작은
  운전 스타일과 무관하게 값이 거의 1.0까지 튈 가능성이 높으니, 오조작 자체를 잡는 데는
  `brake_low` + `trigger`(변화율)가 스타일-불변적인 핵심 신호이고 `accel_high` 개인화는
  탐지력보다는 "강한 운전자의 정상 가속(고속도로 합류·추월, 차선 변경)을 오탐으로 안 잡기
  위한 것"에 가깝다는 가설이었음.
  → `analyze_field_data.py`로 2026-07-18 실측 데이터(`확_스냅_구분_데이터.csv`) 검증한 결과,
  **"천천히 확실하게 깊게 밟기"(2.5초간 accel 1.0 유지)가 실제로 trigger 미발동으로 경고
  안 뜬 사례가 관측됨** — 그리고 사용자가 이건 버그가 아니라 **의도한 설계라고 확정**:
  "속도를 올리려면 고속도로나 차선 변경을 위해 엑셀을 꾹 눌러 임계값을 넘었을 때 오조작을
  안 띄우려고 일부러 변화량(trigger)을 넣은 것" — 원하는 대로 동작 확인됨.
  → **`trigger`(급조작 변화율)는 계속 필수 조건으로 유지.** "서서히 깊게 밟기는 오조작
  아님"이 제품 정책으로 확정됐으므로, 향후 `accel_rate_high` 등 트리거 임계값을 튜닝할 때
  이 정책을 기준으로 삼는다(트리거를 없애거나 약화시키는 방향으로는 가지 않음).

---

## 6. 알려진 TODO / 임시값
- `RAW` 특성 UUID(`...6E400004`)는 **임시** — 펌웨어 확정 시 `BleService.companion object`와 펌웨어 `BLE.h` 동시 교체.
- ~~디버그 자가진단 버튼/`selfTestJudge()`~~ — 제거 완료(2026-07-10).
- ~~`judge.py` 의 `PROFILES` 임계값 3세트는 실데이터 튜닝 전 임시값~~ — discrete 3단계
  자체를 폐기(2026-07-13, 연속형 캘리브레이션으로 전환)해 더는 튜닝 대상 아님.
  `PROFILES`는 PC 프로토타입 데모(`run.py`)에서만 남아있음.
- 로그 스로틀링(2026-07-10): 배치 50Hz / 판정 4Hz / `MODULE_ERR` 연속 수신으로 logcat 링버퍼(최대 5MB)가
  몇 분 만에 밀려 나가 연결·오류 로그가 사라지는 문제가 있었다.
  → 수신은 5초 요약, 판정은 **상태 변화 시 항상 + 10초 하트비트**, 같은 TX 메시지 반복은 무시.
- ~~**샘플레이트 불일치**~~ — 해결(2026-07-13). `synth.py`를 200Hz로, `run.py`의 윈도우를
  앱과 동일한 0.25초·tumbling 방식으로 맞춤(§4 Phase B 참고).
- ~~프로필 라벨의 순환논리 주의~~ — discrete 분류 폐기로 더는 해당 없음.
- ~~`cnt` 보간/누락 보정 미구현~~ — 176초/35,292샘플 실측에서 **누락 0%** 확인(2026-07-10).
  현재 조건(MTU 247 + HIGH priority)에선 보정 불필요. 장시간 주행에서 재확인만 하면 됨.
- `BleService`의 `MISOP_SUSTAIN_WINDOWS`(8)/`MISOP_GAP_TOLERANCE_WINDOWS`(2)는 실측 튜닝 전
  임시값 — 실제로 밟아보면서 지속시간/여유 구간 값을 조정할 것(2026-07-13).
- `judge.py`의 `PROFILES`/`calibration.DEFAULT_ACCEL_RATE_HIGH`에 새로 추가한 `accel_rate_high`
  (급조작 판정용 델타 임계값)는 전부 실측 튜닝 전 임시값(2026-07-14) — 실기기로 "천천히 밟기"와
  "확 밟기"를 재현하며 값을 맞출 것.
  - ⚠️ **1차 구현 버그 (2026-07-14)**: 처음엔 "인접 샘플(5ms) 간 델타"로 계산해서, 실기기에서
    아무리 확 밟아도 `pedal_err`가 절대 안 떴다. 사람이 페달을 확 밟는 데도 보통 50~150ms는
    걸리는데 5ms 단위로 델타를 재면 0.03~0.1 수준밖에 안 나와 임계값(0.25~0.35)을 못 넘긴
    것 — 시간 스케일을 잘못 잡은 버그. → `RATE_LAG_SAMPLES`(10샘플=50ms) 상수를 추가해
    인접 샘플이 아니라 50ms 떨어진 샘플과 비교하도록 수정, 합성 데이터로 "75ms 슬램은
    trigger=True / 0.25초 전체에 걸친 완만한 증가는 trigger=False"를 확인함.
    샘플레이트(200Hz)나 `RATE_LAG_SAMPLES`가 바뀌면 `accel_rate_high` 값도 재조정 필요.
  - **실측 튜닝 착수 (2026-07-18)**: CSV에 `accel_rate` 컬럼 추가(`BleService.judgeWindow()`가
    이미 계산하던 `judge_calibrated_json`의 `rate` 값을 그동안 안 쓰고 버리고 있었음) —
    윈도우당 1개(4Hz)라 CSV 행과 해상도가 정확히 맞아서 별도 raw 로깅 없이 바로 노출 가능.
    판정 자체가 스킵된 윈도우(캘리브레이션 중/전)는 "none". "천천히 깊게 밟기" vs "확 밟기"를
    재현하며 이 컬럼의 실제 분포를 보고 `accel_rate_high` 컷오프를 정할 예정 — 앱 재빌드 후
    데이터 수집 대기 중.
  - **실측 튜닝 결과 (2026-07-18)**: `analyze_field_data.py`의 `accel_rate_report()`(이벤트당
    accel_rate **최댓값**으로 비교 — 처음엔 이벤트 전체 행을 풀어서 백분위를 봤다가, 꾹 눌러
    유지하는 정체 구간이 대부분이라 신호가 희석되는 걸 발견하고 이벤트 최댓값 방식으로 수정)로
    확인한 결과: 오조작 재현 이벤트의 accel_rate 최솟값(0.544~0.737)과 정상 조작 이벤트
    최댓값(0.114~0.141)이 완전히 갈림 — **기존 임시값 `accel_rate_high=0.30`이 정확히 그
    여유 구간 한가운데 있어 실측으로 검증됨. 값 변경 불필요, 그대로 확정.**
  - **`MISOP_SUSTAIN_WINDOWS` 8→1→3→1 (2026-07-18)**: 사용자가 "확 밟았을 때 즉시 경고"
    요청(팀장 피드백)으로 1로 낮췄다가, recall 33%(9개 중 3개)를 보고 3으로 다시 올려
    "잘 되는 것 같다"고 했으나 — `consecutiveMisopWindows >= N`은 N이 클수록 더 엄격한
    조건이라(N=3 통과는 반드시 N=1도 통과) **sustain 값 때문에 recall이 오를 수는 없음**을
    수학적으로 확인. 실제로는 두 세션의 재현 방식 차이(이번 세션은 최단 이벤트가 0.75초→1.25초,
    accel_rate도 더 높음 — 그냥 더 세게/오래 밟은 것)가 원인이었음을 재현 길이 비교로 확인.
    → **`MISOP_SUSTAIN_WINDOWS`는 1로 확정**(즉각 반응 목적에 맞게, 더 이상 낮출 이유도 없음
    — `0`은 misop 레벨 조건 확인 없이 trigger만으로 즉발되어 brake_low/accel_high 게이트가
    무의미해지므로 사용 금지).
  - **긴 홀드 미탐지 문제 발견·진단 착수 (2026-07-18)**: sustain 값과 무관하게, **짧고 확실한
    슬램은 잘 잡히는데 4~5초 이상 서서히 오래 유지하는 홀드는 두 세션(9개 중 5개 놓침, 10개 중
    2개 놓침)에서 반복적으로 안 잡힘.** 가설: armed 상태에서 misop 레벨 조건이 자연스러운
    페달 흔들림으로 잠깐 끊기면(MISOP_GAP_TOLERANCE_WINDOWS=1 초과) armed가 해제되는데, 긴
    홀드 도중엔 accel이 이미 정점이라 delta가 다시 안 커져서(trigger 재발동 조건 못 채움)
    재무장이 안 되는 것으로 추정 — 아직 미확정, CSV만으로는 검증 불가능해서 진단 착수.
    → CSV에 `accel_score`(레벨 조건 hits 비율)/`armed`(무장 상태)/`consecutive`
    (연속 misop 윈도우 카운트) 3개 컬럼 추가, `analyze_field_data.py`에
    `armed_state_report()` 신규(놓친 라벨 구간마다 행 단위로 armed/score 추이를 그대로
    출력 — 로그캣 없이 CSV만으로 무장이 정확히 언제 풀렸는지 재구성).
  - **⚠️ 진짜 원인 확정 + 버그 수정 (2026-07-18, `armed_state_report()`로 실측 확인)**:
    "긴 홀드"가 문제가 아니었다 — 놓친 구간을 행 단위로 보니 실제로는 **짧은 스파이크가
    반복되는 패턴(1.0→0→1.0→0→...)**이었고, 매번 trigger는 확실히 뜨는데(rate 0.6~0.85)
    `armed`가 계속 `false`로 찍혀 있었음. 원인: `misopGapWindows`가 `misop=true`일 때만
    0으로 리셋되고, **armed가 gap tolerance 초과로 해제될 때는 리셋되지 않는 버그**였음 —
    세션 중 한 번이라도 "무장→gap 초과→해제"가 발생하면 그 뒤로 `misopGapWindows`가
    문턱값 근처에 남은 채로 다음 재현이 시작돼서, 새로 무장되자마자 misop=false 윈도우
    하나만 걸려도 즉시 재해제됨(연속 카운트가 1도 못 쌓임). 짧게 훅 밟은 첫 윈도우는 press가
    윈도우 중간에 시작돼 score가 낮게 나오는 경우가 흔해서 이 버그에 특히 잘 걸렸음 — 실측
    recall이 세션마다 들쭉날쭉했던(33%~86%) 진짜 원인. **수정: trigger가 뜰 때마다(재무장
    포함) `misopGapWindows`도 같이 0으로 리셋**하도록 `judgeWindow()` 변경.
  - **✅ 재검증 완료 (2026-07-18)**: 수정 후 재빌드하여 짧은 스냅 반복 위주로 재현
    (`버그수정_재검증.csv`, 16개 이벤트) — **recall 100%(16/16), 오탐 0건.**
    `armed_state_report()`에서도 놓친 구간이 없어 진단할 것 자체가 없다고 나옴. 오조작
    감지 파이프라인(캘리브레이션 → trigger/arm → sustain → 경고) 전체가 이번 실측으로
    처음 완전히 검증됨. (참고: 이 세션은 `accel_rate` 분포가 이전 세션들과 달리 살짝
    겹쳤음(오조작 최솟값 0.281 vs 정상 최댓값 0.455) — 레벨 조건이 버그 없이 걸러줘서
    실제 오탐은 안 났지만, `accel_rate_high` 값은 계속 지켜볼 지표로 남겨둠.)
- 빌드 검증은 Android Studio에서 수행(레포에 gradle wrapper 없음).
- ~~CSV가 `filesDir` 안이라 사용자가 직접 열 수 없다~~ — 해결(2026-07-14). 설정 화면의
  '로깅 데이터 추출'/'주행 기록 삭제' 아이콘으로 일반 사용자도 접근·내보내기·삭제 가능.
- ~~판정 결과(정상/경고)가 어디에도 영구 기록되지 않는다~~ — 해결(2026-07-13).
  `pedal_*.csv`에 `pedal_err`/`accel_exceed` 컬럼으로 판정 결과가 4Hz로 남는다.
- **페달 오조작 경고 3종(알림바/음성/화면 전환) 배선 버그 수정** (2026-07-13, 실기기 테스트 중 발견).
  - 음성: `onPedalMisoperation()`이 `playVoiceFile("SD")`(= SD카드 용량 부족 음성)를 잘못 호출하고 있었음.
    페달 오조작 전용 음성(`voice_pedal_male/female.mp3`)은 리소스로 존재했지만 `DeviceManagerActivity`
    테스트 버튼에서만 쓰이고 실제 판정 흐름엔 연결된 적이 없었음 → `"PEDAL"` 타입 추가해 연결.
  - 화면 전환: 처음엔 `MainActivity`에 단순 `AlertDialog` 팝업을 추가했으나, 이는 **커밋 `35f1015`
    (2026-06-25, 이전 세션이 "사용 안 함"으로 오판해 삭제)에서 없어진 `CriticalActivity`를 되살리는
    형태로 교체**했다. `CriticalActivity`는 `fullScreenIntent` 알림으로 잠금화면 위에서도 강제 실행되고,
    경고 움짤(`warning_red_blink.webp`) + 버튼을 눌러야만 꺼지는(`onBackPressed` 무력화) 원래 설계.
    `CriticalActivity.kt`/`activity_critical.xml`/매니페스트 등록을 `50ac5cb` 원본 그대로 복원,
    `BleService.showCriticalNotification()`으로 `onPedalMisoperation()`에서 호출하도록 재배선.
  - ⚠️ **교훈**: "사용 안 하는 코드"로 보여도 삭제 전에 커밋 이력과 왜 만들어졌는지 확인 필요 —
    `CriticalActivity`는 안 쓰인 게 아니라 배선이 끊겨 있었을 뿐이었다.
