# PeOb 코드 구조 공부 자료

> 목적: 잠시 안드로이드 측을 맡아 작업하는 입장에서, **전체가 어떻게 맞물려 도는지**를
> 동작 원리 수준에서 이해하기 위한 자료. 파일 위치/라인은 실제 코드 기준.

---

## 0. 한 줄 요약

> **BLE 기기(ESP32-C3)가 센서값을 폰으로 쏘고 → 안드로이드(Kotlin)가 받아서 →
> 판정(Python)을 거쳐 → 위험하면 경고를 띄운다.**

옛 구조는 "MCU가 직접 오조작을 판단해서 문자열로 알려줌"이었고,
지금 구조는 "MCU는 날(raw) 센서값만 보내고, **판단은 폰이 로컬에서** 한다"로 바뀌는 중이다.

---

## 1. 전체 데이터 흐름 (이 그림 하나만 머리에 박으면 됨)

```
 ┌──────────────┐   BLE Notify     ┌─────────────────────────────┐   함수호출   ┌──────────────┐
 │  ESP32-C3    │ ───────────────▶ │   Android  (Kotlin)         │ ─────────▶ │  Python      │
 │  (펌웨어)    │   ① 문자열(TX)   │   BleService.kt             │  윈도우    │  judge.py    │
 │              │   ② raw(RAW)     │                             │ ◀───────── │              │
 │              │ ◀─────────────── │   ③ 명령/시간동기(RX)       │   판정결과  │ (Chaquopy)   │
 └──────────────┘   Write          └─────────────────────────────┘            └──────────────┘
                                              │ 위험 판정 시
                                              ▼
                                     알림/소리/팝업 (사용자 경고)
```

세 갈래 채널(NUS):
- **TX** (기기→폰, 문자열): "MODULE_ERR" 같은 간헐적 이벤트. 개발 편의상 문자열.
- **RAW** (기기→폰, 바이너리): ~50Hz 엑셀/브레이크 센서값. 양 많음 → raw.
- **RX** (폰→기기, 바이너리): 시간동기(16B), 에러해제(0xAA), 용량확인(0xAB).

> ⚠️ 엑셀/브레이크 **오조작 판정은 더 이상 TX 문자열로 오지 않는다.**
> RAW로 받아서 **폰이 직접** 판단한다. (이게 이번 구조 변경의 핵심)

---

## 2. 안드로이드 파일 지도

| 파일 | 역할 | 한마디 |
|---|---|---|
| `MainActivity.kt` | 메인 화면 / 시작·정지 / 팝업 | 사용자 눈에 보이는 부분 |
| `BleService.kt` | **BLE 통신 본체** (백그라운드) | ★ 여기가 심장. 제일 많이 봐야 함 |
| `ScanActivity.kt` | 기기 검색·등록 | 주소(MAC) 저장 |
| `DeviceManagerActivity.kt` | 설정(음성 성별 등) | |
| `AndroidManifest.xml` | 권한·구성요소 선언 | 서비스/액티비티 등록부 |

**왜 Service인가?** 화면을 꺼도 BLE 감시가 계속 돌아야 하므로,
Activity(화면)가 아니라 **Foreground Service**가 통신을 담당한다.
(`MainActivity`가 `startForegroundService(BleService)`로 켜고 끈다 → `MainActivity.kt:212`, `:218`)

---

## 3. BleService 내부 흐름 — "연결되면 무슨 일이 순서대로 일어나나"

이게 BLE 코드의 90%다. 순서대로 따라가자. (괄호는 해당 함수)

```
1. onStartCommand        : 주소 받고, 포그라운드 알림 띄우고, 스캔 시작
        │                  (BleService.kt:60)
        ▼
2. startTargetScan       : 저장된 MAC 주소로만 필터링해서 스캔
        │                  (:130)  → ScanFilter.setDeviceAddress
        ▼
3. scanCallback          : 기기 찾으면 스캔 멈추고 연결
        │                  (:153)
        ▼
4. onConnectionStateChange: 연결되면 서비스 탐색(discoverServices) 시작
        │                  (:173)
        ▼
5. onServicesDiscovered  : NUS 서비스에서 TX/RAW 특성 찾기
        │                  (:185)  TX 없으면 = 잘못된 기기(UUID 불일치)
        │                          찾은 특성을 notifyQueue에 [TX, RAW] 적재
        ▼
6. subscribeNext ↔ onDescriptorWrite  ← ★ 여기가 까다로운 부분 (아래 3-1)
        │                  (:233, :218)
        ▼
7. (구독 다 끝나면) sendTimeSyncBinary : 폰 시각을 기기에 전송
                           (:407)
```

이후부터는 **수신 대기 상태**. 기기가 뭔가 보내면 `onCharacteristicChanged`(:222)가 호출됨.

### 3-1. 왜 큐(notifyQueue)로 하나씩 구독하나? (꼭 이해할 것)

BLE의 GATT 작업(구독 설정 등)은 **한 번에 하나씩만** 처리된다(직렬화).
TX, RAW 두 개를 연달아 `enableNotification` 하면 **두 번째가 무시**된다.

그래서:
```
구독요청(TX) ──▶ 기기 응답 ──▶ onDescriptorWrite 콜백 ──▶ 다음 것(RAW) 구독요청 ──▶ ...
```
"한 건 끝났다는 콜백(`onDescriptorWrite`)을 받고 나서 다음 걸 요청"하는 식으로
**줄을 세운다.** 그 줄이 `notifyQueue`(:36)이고, 다 비면 시간동기를 보낸다(:235).

> 예전엔 `postDelayed(500ms)`로 "대충 0.5초 기다렸다 보내기"였는데,
> 그건 기기가 느리면 깨진다. 콜백 체이닝이 정석.

### 3-2. 수신 분기 (onCharacteristicChanged :222)

```kotlin
when (characteristic.uuid) {
  NUS_TX   -> handleTextMessage(...)   // 문자열  → 에러 처리
  RAW_DATA -> handleRawData(...)       // 바이너리 → ★ 로컬 판정 입구
}
```

- `handleTextMessage` (:247): 문자열 보고 분기. 지금은 `MODULE_ERR` 예시 1개만.
- `handleRawData` (:275): **지금은 바이트 수만 로그**. ← **여기에 Python 판정을 붙일 예정.**
  ```
  (할 일) 바이트 → [accel, brake] 디코딩 → 윈도우 버퍼링 → judge() 호출 → 위험하면 경고
  ```

### 3-3. 송신은 전부 writeToRx 하나로 (:379)

폰→기기로 보내는 건 종류 불문 `writeToRx(바이트)` 한 함수로 통일.
- 시간동기: 16바이트 (`sendTimeSyncBinary` :407)
- 에러해제: `0xAA` (`handleErrorClearSequence` :94)
- 용량확인: `0xAB` (`handleCapacityClearSequence` :108)

기기 쪽은 **받은 바이트 길이로 무슨 명령인지 구분**한다(16B=시간, 1B=명령).

---

## 4. Python 프로토타입 구조 (`prototype/`)

Android의 `handleRawData`가 나중에 호출할 **판정 로직을 PC에서 먼저 검증**하는 공간.

```
prototype/src/
├── synth.py      합성 50Hz 데이터 (정상 / 오조작)   ← 실데이터 오면 CSV로 교체
├── windowing.py  슬라이딩 윈도우                    ← 실제 앱에선 Kotlin이 담당
├── judge.py      ★ judge(samples) -> 판정결과        ← 이게 Chaquopy로 그대로 이식됨
└── run.py        synth → window → judge 파이프라인 데모
```

실행: `cd prototype && uv run python src/run.py`

### 4-1. judge의 "계약"이 제일 중요

```python
judge(samples)  # samples = [[accel, brake], [accel, brake], ...]
   → {"misop": bool, "score": float, "reason": str}
```

- 입력/출력을 **기본 자료형(리스트/dict)** 으로만 유지 → Kotlin에서 넘기기 쉬움.
- 알고리즘을 바꾸고 싶으면 **`judge.py` 안만** 고치면 됨. (PC·앱 동시 반영)
- 지금 로직은 예시 휴리스틱: "엑셀 깊게+브레이크 안 밟음" 비율이 50% 넘으면 오조작.

---

## 5. 안드로이드 ↔ 파이썬, 어디서 만나나 (Chaquopy 경계)

핵심: **역할 분담**이 명확하다.

| 일 | 누가 |
|---|---|
| BLE 수신, 바이트 디코딩, **윈도우에 쌓기** | **Kotlin** (`handleRawData`) |
| 쌓인 윈도우를 **판정** | **Python** (`judge`) |
| 판정 결과로 경고 띄우기 | **Kotlin** |

즉 Python은 "**순수 판정기**"다. BLE도 모르고 안드로이드도 모른다. 윈도우 받아 결과만 돌려줌.

```
[Kotlin] handleRawData
   바이트 ──디코딩──▶ buffer.add([accel, brake])
   buffer 꽉 차면 ──▶ python.judge(buffer)  ──▶ {"misop": true} ──▶ 경고!
                          (Chaquopy 다리)
```

**전환이 쉬운 이유**: PC 프로토타입의 `judge.py`를 `app/src/main/python/`에 그대로 복사하고,
데이터 공급원만 "CSV/합성" → "Kotlin이 넘긴 윈도우"로 바꾸면 끝. 로직은 안 건드림.

---

## 6. 공부 추천 순서

1. **이 문서의 §1 그림** 먼저 외우기 (채널 3개 방향).
2. `BleService.kt`를 **§3 순서대로** 따라 읽기 (onStartCommand → ... → onCharacteristicChanged).
   - 특히 §3-1 큐 구독 체이닝은 BLE 특유의 함정이라 꼭 이해.
3. `prototype/src/run.py` 실행해보고, `judge.py` 임계값 바꿔가며 결과 변화 관찰.
4. `handleRawData`(:275)와 `judge`(judge.py)가 **나중에 어떻게 연결될지**(§5) 머릿속에 그리기.

### 핵심 용어 빠른 사전
- **GATT**: BLE에서 데이터 주고받는 규약. "특성(Characteristic)" 단위로 값을 읽고/쓰고/구독.
- **Notify**: 기기가 값 바뀔 때마다 폰에 자동으로 밀어주는 것 (폰이 안 물어봐도 옴).
- **CCCD (0x2902)**: "이 특성 Notify 켤게요" 하고 기기에 알리는 스위치(디스크립터).
- **MTU**: 한 번에 보낼 수 있는 최대 바이트. 기본 23(=실데이터 20B). RAW 많으면 키워야 함(미구현).
- **Foreground Service**: 화면 꺼도 죽지 않는 백그라운드 작업 (상단 알림 필수).
- **Chaquopy**: 안드로이드 앱 안에 Python 인터프리터를 넣어 돌리는 도구.

---

## 7. 지금 미완성/주의 (코드 읽다 헷갈릴 부분)

- `handleRawData`(:275): **아직 로그만**. 판정 로직 미연결.
- `handleTextMessage`(:247): **MODULE_ERR 예시 1개만**. 에러 구조 개편 예정(SD_SMALL 등 TODO).
- `RAW_DATA` UUID(:45): **임시값**(`...0004`). 펌웨어와 확정 필요.
- `requestMtu`: **미구현** → 현재 20바이트 제한. RAW 본격화 시 추가해야 함.
- `isCapacityDialogShowing` 등 용량 관련: 새 에러 구조와 함께 재정비 예정(현재 일부 TODO).
```

> 더 깊은 BLE/펌웨어 규격은 `BLE_NUS_마이그레이션.md` §5 (ESP32-C3 연동 규격) 참고.
