# PeOb 코드 구조 · 공부 자료

> 목적: 이 저장소에서 **코드를 직접 쓰기 위해** 필요한 배경지식과, 실제로 부딪혀서 알게 된
> 노하우(함정과 그 회피법)를 모아둔 문서. 파일 위치/라인은 실제 코드 기준.
>
> 읽는 순서 추천: §1 그림 → §3 BleService → §4 판정기 → §7 증거자료화 → §8 함정 사전 → 나머지.

---

## 0. 한 줄 요약

> **BLE 기기(ESP32-C3)가 raw 센서값을 폰으로 쏘고 → 안드로이드(Kotlin)가 디코딩·윈도잉해서
> → Python(`judge.py` + `calibration.py`)이 "이 사람 기준"으로 판정하고 → 위험하면 폰이 경고한다.**

옛 구조는 "MCU가 직접 오조작을 판단해서 문자열로 알려줌"이었고,
지금은 "MCU는 날(raw) 센서값만 보내고, **판단은 폰이 로컬에서, 그 사람 캘리브레이션 값으로** 한다".

> ⚠️ **2026-07-13에 개인화 방식이 통째로 바뀌었다.** 처음엔 "강함/보통/약함" 3단계 중 하나를
> 사용자가 고르는 방식이었는데, 실측해보니 보통↔강함 경계가 자꾸 겹쳐 불안정했다. 지금은
> **캘리브레이션 주행(30초) → 그 사람의 `accel_active_p90` + 오프셋으로 임계값을 직접 계산**하는
> 연속형 방식으로 완전히 전환됐고, discrete 3단계 관련 코드(`profile.py`, 토글 버튼,
> `sessionProfile` 등)는 전부 삭제됐다. 이 문서 아래 내용은 전부 **연속형 캘리브레이션 기준**이다.

---

## 1. 전체 데이터 흐름

```
 STM32(FSR)  ──UART 200Hz──▶  ESP32-C3  ──BLE Notify 50Hz(4샘플 배치)──▶  Android
                                                                            │
       ┌────────────────────────────────────────────────────────────────────┤
       │                                                                    │
       ▼                                                                    ▼
  BleService.kt                                                    DataCollectActivity
  ├ RAW 디코딩 (Q31 → 0.0~1.0)                                     ├ 실시간 그래프
  ├ CSV 로깅 (pedal_*.csv, 판정 윈도우당 1행)                        ├ 임계선 표시(캘리브레이션 값)
  ├ 윈도우 50샘플 버퍼링                                            ├ "패턴 파악"(캘리브레이션) 버튼
  ├ 캘리브레이션 안 됐으면 판정 자체를 건너뜀                          └ 캘리브레이션 초기화 버튼
  └ judge_calibrated_json(윈도우, 캘리브레이션 임계값)
       │        └▶ [Chaquopy] judge.py ──▶ {misop, score}
       │ misop=true
       ▼
  음성(PEDAL) + 헤드업 알림 + CriticalActivity(강제 전면 경고) + 브로드캐스트
```

세 갈래 GATT 채널(NUS):

| 채널 | UUID 끝자리 | 방향 | 내용 |
|---|---|---|---|
| **TX** | `...0003` | 기기 → 폰 | 문자열 이벤트 (`MODULE_ERR`). 간헐적이라 문자열로 충분 |
| **RAW** | `...0004` ⚠️임시 | 기기 → 폰 | 50Hz 배치 바이너리. 양이 많아 raw |
| **RX** | `...0002` | 폰 → 기기 | 명령 1바이트 (`0xAA` 에러해제, `0xAB` 용량확인) |

> ⚠️ **오조작 판정은 TX 문자열로 오지 않는다.** RAW로 받아 **폰이 직접** 판단한다.
> 시간 동기화(16B)는 규격 §5에서 **제거**됐다. 절대시각은 "폰 수신시각 + cnt"로 역산한다.
>
> ⚠️ **캘리브레이션 전에는 오조작 판정 자체가 동작하지 않는다**(2026-07-13 정책 변경, §3-5 참고).
> 그래서 `MainActivity`가 연결될 때마다 캘리브레이션 안 된 사용자에게 "패턴 파악" 유도 팝업을 띄운다.

---

## 2. 파일 지도

### 안드로이드 (`app/src/main/java/com/example/agproject/`)

| 파일 | 역할 | 한마디 |
|---|---|---|
| `BleService.kt` | **BLE 통신 + 디코딩 + 윈도잉 + 캘리브레이션 + 판정 호출 + CSV 로깅** | ★ 심장. 935줄. 여기가 90% |
| `MainActivity.kt` | 메인 화면 / 감시 시작·정지 / 캘리브레이션 온보딩 팝업 | 사용자 진입점 |
| `DataCollectActivity.kt` | 실시간 그래프 + 임계선 + "패턴 파악"(캘리브레이션) + 라벨링 | 개인화 + Phase A 데이터 수집용 |
| `PedalGraphView.kt` | Canvas 직접 그리는 커스텀 View (링 버퍼) | 차트 라이브러리 미사용 |
| `ScanActivity.kt` | 기기 검색·등록 (MAC 저장) | |
| `DeviceManagerActivity.kt` | 설정 (음성 성별, 수집 화면 진입, **로그 추출/삭제**) | 서명된 증거자료 내보내기(§7) |
| `EvidenceSigner.kt` | Android Keystore EC 키로 CSV 서명 (Phase E) | 개인키는 Keystore 밖으로 안 나감 |
| `CriticalActivity.kt` | 오조작 발생 시 강제 전면 경고 화면 | 잠금화면 위에서도 뜸, 뒤로가기 무력화 |
| `AndroidManifest.xml` | 권한·구성요소 선언 | 새 Activity는 여기 등록 필수 |

### Python (`app/src/main/python/` ↔ `prototype/src/`, 바이트 단위 동일 유지)

| 파일 | 역할 |
|---|---|
| `judge.py` | **오조작 판정 코어.** 계산은 `judge_with_thresholds`, 앱은 `judge_calibrated_json` 경로 사용 |
| `calibration.py` | **개인별 임계값 계산.** `accel_active_p90 + 오프셋`으로 `accel_high` 산출 |
| `features.py` | 페달 압력 시계열 → 요약 통계(feature) 추출. `calibration.py`가 내부에서 호출 |
| `synth.py` (prototype 전용) | 합성 데이터 (정상/오조작), 200Hz 기준 |
| `windowing.py` (prototype 전용) | 슬라이딩 윈도우 (앱에선 Kotlin이 담당) |
| `run.py` (prototype 전용) | 파이프라인 데모, discrete `PROFILES` 기반 |
| `verify_signature.py` (양쪽에 사본) | PC/제3자용 서명 검증 스크립트 — stdlib만 사용(§7) |

> ⚠️ **`judge.py`/`calibration.py`/`features.py`/`verify_signature.py`는 앱·prototype 두 벌이
> 반드시 같아야 한다.** 지금은 수동 복사. 고칠 땐 양쪽 다.
> `diff prototype/src/judge.py app/src/main/python/judge.py` 로 확인하는 습관.
>
> `prototype/src/profile.py`(discrete 3단계 분류)는 **삭제됐다.** 앱에 연결된 적도 없어
> 안전하게 제거된 죽은 코드였음(2026-07-13).

**왜 Service인가?** 화면을 꺼도 BLE 감시가 계속 돌아야 하므로, Activity가 아니라
**Foreground Service**가 통신을 담당한다. `MainActivity`에서 `startForegroundService()`.

---

## 3. BleService 내부 흐름

### 3-1. 연결 시퀀스 — "연결되면 무슨 일이 순서대로 일어나나"

```
1. onStartCommand (:176)      action 분기(에러/용량/라벨/스트림/캘리브레이션) 먼저, 없으면 연결 시작
        ▼
2. startTargetScan (:289)     저장된 MAC으로만 필터링 (ScanFilter.setDeviceAddress)
        ▼
3. scanCallback (:312)        기기 찾으면 스캔 멈추고 connectGatt
        ▼
4. onConnectionStateChange (:329)   연결됨 → discoverServices()
        ▼
5. onServicesDiscovered (:~340)     NUS에서 TX/RAW 특성 찾기 → notifyQueue에 [TX, RAW] 적재
        │                          그리고 requestMtu(247)  ← ★ 구독보다 먼저!
        ▼
6. onMtuChanged (:~360)             MTU 협상 끝 → requestConnectionPriority(HIGH) → subscribeNext
        ▼
7. subscribeNext ↔ onDescriptorWrite   큐가 빌 때까지 하나씩 구독
        ▼
8. 수신 대기 → onCharacteristicChanged (:~390)
```

`onStartCommand()`에서 연결 시작 직전에 **이전 캘리브레이션 값을 prefs에서 복원**한다
(`applyCalibratedThresholds(readCalibratedThresholds())`, :238) — 매번 재캘리브레이션하지 않도록.

### 3-2. ★ 왜 MTU를 구독보다 먼저 요청하나 (데이터 0의 1순위 원인)

MTU를 요청하지 않으면 기본 **23바이트** → notify payload는 **20바이트**.
그런데 RAW 배치 프레임은 `1 + 4×10 = 41바이트`. **아예 안 들어온다**
(펌웨어가 에러 리턴하고 무전송 — 앱 쪽엔 로그조차 안 남아서 원인 찾기 고약함).

```kotlin
gatt.requestMtu(247)   // onServicesDiscovered 끝, 구독 시작 전
```
→ 결과는 `onMtuChanged`로 오고, **거기서** 구독을 시작한다. 실제 MTU는 양측 협상값의 min.

덧붙여 `requestConnectionPriority(CONNECTION_PRIORITY_HIGH)`.
50Hz notify를 빠짐없이 받으려면 connection interval ≤ 20ms가 필요하다(HIGH면 11.25~15ms).

### 3-3. ★ 왜 큐(notifyQueue)로 하나씩 구독하나

BLE의 GATT 작업은 **한 번에 하나씩만** 처리된다(직렬화).
TX, RAW를 연달아 `enableNotification` 하면 **두 번째가 조용히 무시**된다.

```
구독요청(TX) ─▶ 기기 응답 ─▶ onDescriptorWrite 콜백 ─▶ 다음 것(RAW) 구독요청 ─▶ ... ─▶ 큐 빔
```

"한 건 끝났다는 콜백을 받고 나서 다음 걸 요청"하는 **콜백 체이닝**.

> 예전엔 `postDelayed(500ms)`로 "대충 기다렸다 보내기"였다. 기기가 느리면 깨진다. 콜백 체이닝이 정석.

### 3-4. RAW 디코딩 (handleRawData :573)

```
프레임 = count(1B) + RawSample × count       ← 전부 little-endian
RawSample(10B) = int32 Q31 brake | int32 Q31 accel | uint16 cnt
                 offset 0          offset 4          offset 8
```

핵심 코드:
```kotlin
val buf = ByteBuffer.wrap(data).order(ByteOrder.LITTLE_ENDIAN)
val count = buf.get().toInt() and 0xFF           // 부호 없는 바이트로
if (data.size != 1 + count * 10) return          // 길이 검증 → 깨진 프레임 폐기
repeat(count) {
  val brkRaw = buf.int                            // ★ brake 가 먼저!
  val accRaw = buf.int
  val cnt = buf.short.toInt() and 0xFFFF          // 부호 없는 short 으로
  val brake = brkRaw / Q31_SCALE                  // 2^31 로 나눠 0.0~1.0
  val accel = accRaw / Q31_SCALE
  sampleBuffer.add(listOf(accel, brake))          // ★ judge 는 [accel, brake] 순서!
  if (isCalibrating) calibrationBuffer.add(...)   // 캘리브레이션 중이면 별도 버퍼에도 적재
}
```

여기서 **순서가 두 번 뒤집힌다**: 바이트 스트림은 `brake, accel` 순인데
`judge.py`는 `[accel, brake]`를 기대한다. 헷갈리기 딱 좋으니 주의.

**cnt 누락 검출**: 200Hz 등간격이라 직전과의 차가 1보다 크면 그만큼 빠진 것.
wrap-around(65536) 때문에 `(cnt - lastCnt) and 0xFFFF` 로 계산한다.

`handleRawData`는 캘리브레이션 지속시간(`CALIBRATION_DURATION_MS`)이 지나면 자동으로
`finishCalibration()`도 호출한다 — 사용자가 따로 "종료" 버튼을 안 눌러도 된다.

### 3-5. 윈도잉 → 판정 (judgeWindow :665)

```
sampleBuffer 에 계속 쌓기 → 50개(WINDOW_SIZE) 차면 → 복사본 만들고 clear → judge_calibrated_json 호출
```

50샘플 @200Hz = **0.25초 윈도우**, 판정은 4Hz로 나온다. (WINDOW_SIZE는 튜닝 대상)

```kotlin
fun judgeWindow(window: List<List<Double>>) {
    if (isCalibrating) return                              // 캘리브레이션 중엔 판정 안 함
    val calibrated = calibratedThresholdsJson ?: return     // ★ 캘리브레이션 전이면 판정 자체를 건너뜀
    val result = judge.callAttr("judge_calibrated_json", samplesJson, calibrated)
    // result.profile 은 항상 "personalized" 문자열(3단계 프로필명 아님)
}
```

> ⚠️ **정책 변경(2026-07-13)**: 처음엔 캘리브레이션이 없으면 `normal` 프로필로 폴백했는데,
> 그러면 "패턴 파악" 도중 의도적으로 세게 밟는 순간에도 오조작 경고가 떠서 캘리브레이션 자체가
> 불편했다. → **"임계값 미설정 = 판정 안 함"**으로 확정. 트레이드오프: 한 번도 캘리브레이션
> 안 하면 오조작 감지가 아예 동작하지 않는다 — 그래서 `MainActivity`의 강제 온보딩 팝업이
> 사실상 필수가 됐다(§6-2).

`lastMisopShown`으로 같은 오조작이 연속 트리거되는 걸 막는다.
정상으로 돌아오면 다시 false → 다음 오조작에 또 경고.

### 3-6. 캘리브레이션 3단계

```
startCalibration()          isCalibrating=true, calibrationBuffer 비움,
                             기존 calibratedThresholdsJson 을 즉시 null 로 되돌림
        ▼ (30초, CALIBRATION_DURATION_MS 동안 handleRawData 가 계속 버퍼링)
finishCalibration()          calibration.calibrate_thresholds_json(buffer) 호출
                             → 결과를 SharedPreferences(PREF_CALIBRATED_THRESHOLDS)에 저장
                             → applyCalibratedThresholds() 로 즉시 적용
                             → ACTION_CALIBRATION_DONE 브로드캐스트 (accel_high 등 포함)
```

`applyCalibratedThresholds(json)`은 `calibratedThresholdsJson`과 `currentAccelHigh`(CSV용 캐시,
아래 §3-7)를 **항상 같이** 갱신한다 — 둘을 따로 업데이트하면 CSV의 `accel_exceed`가 어긋난다.

`ACTION_CLEAR_CALIBRATION`(초기화 버튼)은 `applyCalibratedThresholds(null)` + prefs 제거로
"캘리브레이션 전" 상태로 되돌린다 — 이후 다시 판정이 멈춘다.

### 3-7. CSV 로깅 — 판정 이벤트 겸 증거자료 (ensureCsvWriterLocked :449, writeRawCsv :529)

```
filesDir/logs/pedal_yyyyMMdd_HHmmss.csv
컬럼: date,time,brake,accel,module_err,pedal_err,accel_exceed,label
```

- **판정 윈도우(0.25초)당 1행** — 원시 샘플 200Hz 전부가 아니다(처음엔 200Hz로 찍었다가
  "시간당 수십만 행"이라는 실기기 피드백으로 4Hz 병합 확정, 2026-07-13). `brake`/`accel`은
  그 윈도우의 마지막 샘플값을 대표값으로 쓴다. 캘리브레이션은 CSV가 아니라 메모리 버퍼에서
  200Hz 그대로 계산하므로 정확도엔 영향 없다.
- `module_err`: `"module_err"`/`"none"` — 그 순간 미해결 장치 오류 여부.
- `pedal_err`: `"pedal_err"`/`"none"` — `judge_calibrated_json`의 misop과 동일 개념(컬럼명만
  "열어봤을 때 바로 알아볼 수 있게" 사람이 읽는 문자열로 통일).
- `accel_exceed`: 대표 accel이 그 순간 `currentAccelHigh`를 넘은 양(캘리브레이션 전이거나
  안 넘었으면 0). `currentAccelHigh`를 캐시해두는 이유는 200Hz마다 JSON을 다시 파싱하는
  낭비를 피하기 위해서(§3-6).
- `label`: 개발자용 수동 라벨링(지도학습 정답, Phase A) — 판정과 무관하게 그대로 유지.

**lazy 생성**: 첫 RAW 샘플이 실제로 도착했을 때 파일을 연다.
→ 연결 실패 시 **빈 파일이 안 남는다**.

> ⚠️ 파일명에서 `_<profile>` 접미사(강/보통/약)는 **없어졌다**(2026-07-13, discrete 폐기와 함께).
> 2026-07-13 이전 파일만 `pedal_..._normal.csv`처럼 접미사가 붙어 있다.

### 3-8. 송신은 전부 writeToRx 하나로

폰→기기는 종류 불문 `writeToRx(바이트)`. 에러해제 `0xAA`, 용량확인 `0xAB`.
기기는 **받은 바이트 길이/값으로 명령을 구분**한다.

### 3-9. Intent action 라우팅 패턴

`onStartCommand`는 "서비스 시작"뿐 아니라 **살아있는 서비스에 명령을 보내는 통로**로도 쓴다.

```kotlin
if (intent?.action == ACTION_SET_LABEL) { currentLabel = ...; return START_NOT_STICKY }
if (intent?.action == ACTION_START_CALIBRATION) { startCalibration(); return START_NOT_STICKY }
if (intent?.action == ACTION_CLEAR_CALIBRATION) { applyCalibratedThresholds(null); ...; return START_NOT_STICKY }
// 아래로 내려가면 "진짜 시작" 경로
```

Activity 쪽에서 `startService(Intent(...).apply { action = ... })` 로 부른다.
바인딩(`onBind`)을 안 쓰고 이 방식을 택한 이유는 단방향 fire-and-forget 명령이라 간단해서다.
(서비스 → Activity 방향은 반대로 `sendBroadcast`.)

### 3-10. 오조작 경고 3종 배선 (onPedalMisoperation :710)

```
onPedalMisoperation()
  ├─ playVoiceFile("PEDAL")            페달 오조작 전용 음성(성별 설정 반영)
  ├─ updateNotification / showCriticalNotification()   fullScreenIntent 알림(잠금화면에서도 뜸)
  └─ CriticalActivity 를 Intent + FLAG_ACTIVITY_NEW_TASK 로 직접 실행
       (foreground 화면에선 fullScreenIntent 가 자동으로 안 뜰 수 있어 이중으로 실행)
```

> ⚠️ **과거 버그(2026-07-13 발견·수정)**: `playVoiceFile("SD")`(용량부족 음성)를 잘못 호출하고
> 있었고, `CriticalActivity`는 이전 세션이 "안 쓰는 코드"로 오판해 삭제했던 걸 복원한 것.
> **교훈**: "사용 안 하는 코드"로 보여도 삭제 전에 커밋 이력과 왜 만들어졌는지 확인할 것.

---

## 4. Python 판정기 (`judge.py` + `calibration.py` + `features.py`)

### 4-1. 계약(Contract) — 코어는 `judge_with_thresholds`

```python
# judge.py
judge_with_thresholds(samples, thresholds: dict, label: str) -> dict   # ★ 실제 판정 로직은 여기 하나뿐
#                      ↑ [[accel, brake], ...]     ↑ {"accel_high":.., "brake_low":.., "high_ratio":..}

judge(samples, profile="normal") -> dict          # 레거시 3단계 경로: get_thresholds(profile) 를 얹은 얇은 래퍼
judge_json(samples_json, profile="normal") -> str # 위의 JSON 어댑터 (prototype run.py 데모용)

judge_calibrated_json(samples_json, thresholds_json) -> str  # ★ 앱이 실제로 쓰는 경로
                                                               # label 은 "personalized" 로 고정
```

- 입출력은 **기본 자료형(리스트/dict/문자열)만**. numpy 객체를 인자로 받지 않는다.
- **핵심 설계**: `judge_with_thresholds`가 "임계값 딕셔너리 하나로 판정한다"는 계약만 지키면,
  그 임계값이 **3단계 프로필에서 왔든(`judge`) 개인별 캘리브레이션에서 왔든(`judge_calibrated_json`)**
  판정 로직 자체는 안 바뀐다. 실제로 2026-07-13에 개인화 방식을 통째로 갈아엎었을 때도
  `judge_with_thresholds`는 한 글자도 안 고쳤다 — 얇은 래퍼만 하나 추가했을 뿐.
- Python은 "**순수 판정기**"다. BLE도 안드로이드도 모른다. 윈도우 받아 결과만 돌려준다.

### 4-2. 개인화 구조 — discrete 폐기, 연속형 확정

| 문제 | 예전(폐기됨) | 지금 |
|---|---|---|
| "이 사람은 어느 프로필인가?" | 사용자가 강/보통/약 토글로 수동 선택 | **없음** — 프로필이라는 개념 자체가 사라짐 |
| "이 사람 기준 임계값은 얼마인가?" | `PROFILES[profile]` 고정 3세트 | **`calibration.calibrate_thresholds(samples)`**로 매번 계산 |
| "지금 이 윈도우가 오조작인가?" | `judge()` | `judge_with_thresholds()` (그대로) |

```python
# judge.py — PC 프로토타입 데모(run.py)용으로만 남아있음. 앱 실시간 판정 경로에선 안 쓰임.
PROFILES = {
    "strong": {"accel_high": 0.92, "brake_low": 0.15, "high_ratio": 0.55},
    "normal": {"accel_high": 0.85, "brake_low": 0.10, "high_ratio": 0.50},
    "weak":   {"accel_high": 0.75, "brake_low": 0.07, "high_ratio": 0.45},
}
```

```python
# calibration.py — 앱이 실제로 쓰는 경로
ACCEL_HIGH_OFFSET = 0.20     # 2026-07-13: 0.10 → 0.20 재조정(실기기 캘리브레이션 후 여유 부족 피드백)
DEFAULT_BRAKE_LOW = 0.10     # brake_low/high_ratio는 아직 개인화 안 함 — PROFILES["normal"]과 동일값 재사용
DEFAULT_HIGH_RATIO = 0.50

def calibrate_thresholds(samples: list[list[float]]) -> dict[str, float]:
    f = extract_features(samples)
    accel_high = min(f["accel_active_p90"] + ACCEL_HIGH_OFFSET, 1.0)
    return {"accel_high": round(accel_high, 4), "brake_low": DEFAULT_BRAKE_LOW, "high_ratio": DEFAULT_HIGH_RATIO}

def calibrate_thresholds_json(samples_json: str) -> str: ...   # Chaquopy 경계 어댑터
```

**왜 `accel_active_p90`인가?** 실기기 세션은 대부분(90%+)이 idle(페달 안 밟음)이라, 세션 전체
평균/percentile로는 "이 사람이 밟을 때 얼마나 깊게 밟는지"를 못 잡는다. `features.py`가
`accel >= 0.1`(밟고 있는 샘플)만 걸러내 percentile을 내는 이유가 여기 있다(§4-3). 9세션 실측
비교에서 `accel_active_p90 + 오프셋` 공식이 `accel_active_mean` 기반보다 훨씬 적은 오프셋
(+0.08~0.10 vs +0.30)으로도 오탐률 2% 이하를 달성 — 이미 그 사람의 "튀는 값"을 반영하고
있어서다(`진행상황_및_로드맵.md` §4 Phase B 실험 기록 참고).

### 4-3. `features.py` — 캘리브레이션이 참고하는 feature 11종

```python
extract_features(samples, sample_rate_hz=200) -> dict
```

| feature | 의미 |
|---|---|
| `accel_mean/std/p90` | 세션 전체(idle 포함) accel 통계 — **실전에선 거의 무의미**(idle에 파묻힘) |
| `brake_mean/std` | 세션 전체 brake 통계 |
| `max_accel_jump` | 급전환 강도(연속 샘플 간 최대 증가폭) |
| `simultaneous_press_ratio` | accel·brake 동시 가압(둘 다 ≥0.1) 비율 |
| `accel_active_mean/p90` | **accel≥0.1인 샘플만** 조건부 통계 — 캘리브레이션이 실제로 쓰는 값 |
| `press_rate_per_min` | 1분당 페달 조작(연속 구간) 횟수 |
| `press_duration_mean_sec` | 한 번 밟을 때 평균 지속시간 |

`calibrate_thresholds`는 지금 이 중 **`accel_active_p90` 하나만** 쓴다. 나머지는 추출은
되지만 아직 개인화(`brake_low`/`high_ratio`)에 반영 전 — Phase D(후순위) 대상.

### 4-4. 임계값의 단일 소스(Single Source of Truth)

그래프의 붉은 임계선도 캘리브레이션 결과(`SharedPreferences`)에서 읽어온다. Kotlin이
`accel_high` 값을 하드코딩하지 않는다.

```
DataCollectActivity.applyThresholdsToGraph(thresholdsJson)
  └─▶ SharedPrefs(PREF_CALIBRATED_THRESHOLDS) 에서 읽은 JSON  →  {"accel_high":.., "brake_low":..}
        └─▶ graph.setThresholds(...)
```

캘리브레이션 전이면 `PedalGraphView.DEFAULT_ACCEL_HIGH/DEFAULT_BRAKE_LOW`(0.85/0.10, 참고용
상수) 로 되돌아간다. `judge.thresholds_json(profile)`은 이제 prototype 데모(`run.py`) 전용이다.

### 4-5. PC 프로토타입 실행

```bash
cd prototype && uv run python src/run.py
```

`synth.py`는 200Hz로 통일됐고(2026-07-13), `run.py`의 `WINDOW_SIZE`도 `SAMPLE_RATE_HZ // 4`
(=50샘플=0.25초, `BleService.WINDOW_SIZE`와 동일)로 맞춰져 있다. **더 이상 샘플레이트/윈도우
불일치 문제는 없다** — 예전엔 "프로토타입 1초 윈도우 ≠ 앱 0.25초 윈도우"라 튜닝값을 그대로
옮기면 안 됐지만, 지금은 옮겨도 된다.

---

## 5. Chaquopy 경계 — 안드로이드와 파이썬이 만나는 곳

### 5-1. 역할 분담

| 일 | 누가 |
|---|---|
| BLE 수신, 바이트 디코딩, **윈도우/캘리브레이션 버퍼에 쌓기** | **Kotlin** (`handleRawData`) |
| 캘리브레이션 임계값 계산 | **Python** (`calibration.calibrate_thresholds_json`) |
| 쌓인 윈도우를 **판정** | **Python** (`judge.judge_calibrated_json`) |
| 판정 결과로 경고 띄우기 | **Kotlin** |

### 5-2. ★ JSON 문자열로 주고받는 이유 (반드시 알 것)

```kotlin
val samplesJson = JSONArray().also { for (s in window) it.put(JSONArray(s)) }.toString()
val resultJson = Python.getInstance()
    .getModule("judge")
    .callAttr("judge_calibrated_json", samplesJson, calibratedThresholdsJson)   // ← 문자열만 넘긴다
    .toString()
```

**Chaquopy는 Java `ArrayList`를 Python `list`로 자동 변환하지 않는다.**
중첩 리스트를 그대로 넘기면 런타임에 `ArrayList object is not iterable` 이 터진다.
JSON 문자열로 직렬화해 넘기면 컬렉션 변환 문제를 **완전히 우회**한다.

그래서 `judge.py`/`calibration.py`에는 항상 두 층이 있다:
- `judge_with_thresholds(samples, thresholds, label)` / `calibrate_thresholds(samples)` — 순수 로직. PC에서도 직접 호출.
- `judge_calibrated_json(samples_json, thresholds_json)` / `calibrate_thresholds_json(samples_json)` — Chaquopy 경계 어댑터. JSON in / JSON out.

### 5-3. 런타임 시작

```kotlin
if (!Python.isStarted()) Python.start(AndroidPlatform(this))
```
앱 생명주기 동안 **한 번만**. `BleService.onCreate`와
`DataCollectActivity`(캘리브레이션/그래프 진입 시) 양쪽에 방어적으로 들어 있다.

### 5-4. 빌드 설정 (`app/build.gradle.kts`)

```kotlin
plugins { id("com.chaquo.python") }
android { defaultConfig { ndk { abiFilters += listOf("arm64-v8a", "x86_64") } } }
chaquopy { defaultConfig { version = "3.12" } }
```
- `abiFilters` 필수 — Python은 네이티브라 ABI를 명시해야 한다 (실기기 arm64 + 에뮬 x86_64).
- Python 코드 기본 경로는 `app/src/main/python/`.
- numpy/sklearn 등 외부 패키지는 **한 번도 필요해진 적 없다** — judge/features/calibration
  전부 순수 Python(stdlib)이라 Chaquopy 패키지 제약 문제 자체가 사라졌다.

---

## 6. 데이터 수집 · 개인화 (Phase A/B) — 캘리브레이션은 어떻게 이뤄지나

### 6-1. "패턴 파악" 버튼 (`DataCollectActivity`)

```
btnCalibrate 클릭 → ACTION_START_CALIBRATION → BleService.startCalibration()
  → 30초 카운트다운(버튼 텍스트 갱신) → BleService.finishCalibration() (자동)
  → ACTION_CALIBRATION_DONE 브로드캐스트 수신 → onCalibrationDone()
  → applyThresholdsToGraph() 로 임계선 즉시 갱신 + 토스트로 계산된 accel_high 표시
```

버튼 옆 `btnResetCalibration`(초기화 아이콘) → 확인 다이얼로그 → `ACTION_CLEAR_CALIBRATION`
→ "재설치한 것처럼" 캘리브레이션 이전 상태로 복귀(안전 관련 동작이라 확인 필수).

### 6-2. 캘리브레이션 강제 온보딩 (`MainActivity`)

```kotlin
// ACTION_UUID_MATCHED(연결 성공) 수신 시
maybeShowCalibrationOnboarding()
  // PREF_CALIBRATED_THRESHOLDS 없으면 → 팝업 → 버튼 누르면 DataCollectActivity 로 이동
  // 있으면 아무것도 안 함
```

캘리브레이션할 때까지 **연결할 때마다 다시 뜬다**(§3-5의 트레이드오프에 대응). 같은 팝업이
중복 표시되지 않도록 `isCalibrationPopupShowing` 플래그로 막는다.

### 6-3. CSV 로깅 (`BleService`, §3-7과 동일 — 여기선 학습 데이터 관점만)

```
filesDir/logs/pedal_20260710_143022.csv
컬럼: date,time,brake,accel,module_err,pedal_err,accel_exceed,label
```
- **판정 윈도우당 1행**(4Hz). 원시 200Hz 전부가 아니다(§3-7 참고 — 처음엔 원시 샘플 1개당
  1줄이었지만 증거자료 스키마로 바뀌면서 4Hz로 재조정됨).
- `filesDir`라 **저장소 권한 불필요**. lazy 생성이라 연결 실패 시 빈 파일이 안 남는다.

### 6-4. 수동 라벨링 (지도학습용, 개발자 전용 영역)

`DataCollectActivity`의 "오조작" 토글 → `ACTION_SET_LABEL` → `currentLabel` 갱신
→ 이후 CSV 행의 `label` 컬럼이 1로 찍힌다.

라벨은 **배치 시작 시점에 한 번만 읽는다**. 배치 처리 도중 토글이 바뀌어도
같은 배치(20ms) 안의 샘플은 같은 라벨을 갖는 편이 해석하기 쉽기 때문.

> 오조작 이벤트 라벨링(지도학습)은 당분간 개발자 전용 영역으로 남겨두기로 함(2026-07-13
> 결정) — 일반 사용자에게 노출되는 건 "패턴 파악"(캘리브레이션) 흐름뿐이다.

### 6-5. 실시간 그래프 (`PedalGraphView`)

- 외부 차트 라이브러리 없이 **Canvas 직접 그리기**. 라인 2개 + 임계선 2개뿐이라 의존성 늘릴 이유가 없다.
- **링 버퍼** 1000샘플(200Hz × 5초). `head`가 다음에 쓸 위치, `size`가 채워진 개수.
  `idx(i)`가 "0=가장 오래된 것"을 실제 인덱스로 바꾼다.
- Y축 0.0~1.0 고정 — Q31 정규화라 범위가 이미 확정.
- 50Hz 브로드캐스트는 **수집 화면이 떠 있는 동안만** 켠다(`ACTION_SET_LIVE_STREAM`). 평소엔 낭비.

---

## 7. 증거자료화 — 로그 서명·추출·삭제 (Phase E)

### 7-1. 왜 "암호화"가 아니라 "서명"인가

> - 암호화 = **기밀성**(남이 못 봄). 증거에 필요한 건 **무결성·진정성**("사고 당시 그 기기에서
>   나왔고, 이후 변조 없음").
> - 사용자가 복호화할 수 있으면 **수정 후 재암호화도 가능** → 암호화만으로는 변조를 못 막는다.
> - 경찰/보험사/법원이 **열 수 없는 파일은 증거 가치가 떨어진다.**
>
> → 채택안: **평문 CSV + 분리 서명(`.sig`)**. Android Keystore의 하드웨어 백업 개인키(추출
>   불가)로 CSV 해시에 서명하고 공개키를 공개. 누구나 열람 가능하되 1바이트만 바뀌어도 검증
>   실패(블랙박스·EDR이 쓰는 방식).

### 7-2. `EvidenceSigner.kt` — 서명 생성

```kotlin
object EvidenceSigner {
    // "peob_evidence_signing_key" 별칭, AndroidKeyStore, secp256r1(P-256), SHA256withECDSA
    fun signFile(file: File): String     // 파일 전체 서명 → base64 문자열(.sig 내용)
    fun publicKeyPem(): String           // Keystore 인증서에서 공개키를 X.509 PEM 으로 추출
}
```

키 쌍은 **최초 서명 시점에 lazy 생성**. 개인키는 하드웨어 보안 저장소 밖으로 절대 안 나간다
— Keystore가 서명 연산만 대행하고, `getKey()`로 돌려주는 건 `PrivateKey` **핸들**일 뿐 원본
키 바이트에는 접근할 수 없다.

### 7-3. `DeviceManagerActivity.exportSession()` — 내보내기

`btnExportLogs` 클릭 → 세션 목록(파일명에서 타임스탬프 추출, `formatSessionLabel()`로
"26/07/13-03:28:47"처럼 사람이 읽기 좋은 형식으로 표시) → 선택 → zip으로 묶어 시스템
공유 시트(`ACTION_SEND`)로 내보냄.

**zip 내용물** (서명 성공 시 5개, 실패 시 CSV만 — 안전한 폴백):
```
pedal_<ts>.csv              원본 CSV(평문)
pedal_<ts>.csv.sig          EvidenceSigner.signFile() 결과(base64)
pubkey.pem                  EvidenceSigner.publicKeyPem()
verify_signature.py         검증 스크립트(assets 에서 복사)
README_검증방법.txt          비개발자용 사용법 안내문(assets 에서 복사)
```

`FileProvider`(`res/xml/file_paths.xml`)로 `filesDir/logs` + `cacheDir`(zip 임시 저장) 노출.

### 7-4. `verify_signature.py` — 제3자(경찰/보험사) 검증

```bash
python3 verify_signature.py pedal_<ts>.csv    # 같은 폴더의 .sig/pubkey.pem 자동 탐색
```

⚠️ **표준 라이브러리만 사용**(`argparse, base64, hashlib, sys, pathlib` — `cryptography` 등
외부 의존성 전혀 없음). ECDSA(P-256/SHA-256) 서명 검증을 **순수 Python으로 직접 구현**했다
(DER 파싱 + 타원곡선 점 덧셈/배가 + `pow(x, -1, p)` 모듈러 역원). 이유: "검증하는 컴퓨터에
pip install조차 안 될 수 있다"는 지적(2026-07-14) — 받는 쪽은 Python 3만 있으면 인터넷
연결도, 저장소 클론도 없이 검증 가능해야 한다. Android Keystore도 동일 표준(X.509 DER/PEM)을
쓰므로 실기기 서명과 호환 확인됨.

`prototype/verify_signature.py`와 `app/src/main/assets/verify_signature.py`는 **바이트 단위로
동일**해야 한다(judge.py 등과 같은 동기화 원칙).

### 7-5. 삭제 기능 (`DeviceManagerActivity`)

`btnDeleteLogs` → 세션 목록 다이얼로그(맨 위 "전체 삭제(N개 세션)" + 개별 세션, `btnExportLogs`와
동일한 형식 재사용) → 선택 시 확인 다이얼로그(되돌릴 수 없음 경고) → 확인해야 실제 삭제.

⚠️ 지금 감시 중인 세션(아직 기록 중인 CSV)을 삭제해도 앱이 죽진 않지만 이후 그 세션 기록은
사라진다 — 현재 세션을 목록에서 구분해 막는 건 1차 구현 범위 밖.

### 7-6. 아직 범위 밖인 것

**공개키 자체의 진정성 증명**(예: 폰 최초 등록 시 공개키를 신뢰 가능한 별도 채널로 공지)은
범위 밖 — 지금은 "같은 zip 안에 공개키 포함"까지만. 검증자가 "이 공개키가 정말 이 특정
폰 것"이라는 건 별도로 신뢰해야 한다(사용자 결정, 2026-07-14).

---

## 8. ★ 함정 사전 — 이 코드베이스에서 실제로 물린 것들

읽다가 "왜 이렇게 짰지?" 싶으면 여기서 찾을 것.

### 8-1. 스레드

**BLE notify 콜백은 여러 바인더 스레드에서 올라온다.** 메인 스레드가 아니다.

| 대상 | 대응 |
|---|---|
| `csvWriter` (BufferedWriter) | `synchronized(csvLock)` 로 전부 감쌈 |
| `currentLabel` | `@Volatile` (BLE 스레드 읽기, UI 스레드 쓰기) |
| `liveStreamEnabled` | `@Volatile` |
| `calibratedThresholdsJson` / `currentAccelHigh` / `isCalibrating` | `@Volatile` (BLE 스레드가 읽고, 캘리브레이션 함수가 씀) |

**`@Volatile`은 원자성이 아니라 가시성 보장이다.** 여기선 단일 쓰기/읽기라 충분하지만,
읽고-수정하고-쓰는(read-modify-write) 값이면 `AtomicInteger` 등이 필요하다.

### 8-2. 종료 순서와 좀비 콜백

```kotlin
override fun onDestroy() {
  disconnectGatt()   // 먼저 GATT 를 끊고
  closeCsvWriter()   // 그 다음 파일 닫기
}
```
**순서가 중요하다.** 반대로 하면 뒤늦게 도착한 notify 콜백이 파일에 쓰려다
`ensureCsvWriterLocked()`를 호출해 **새 세션 파일을 되살린다**.
그걸 막으려고 `csvClosed` 플래그까지 둔 것 — 이중 방어.

### 8-3. logcat 링버퍼 고갈

배치 50Hz + 판정 4Hz로 로그를 찍으면 **몇 분 만에 링버퍼가 밀려 나가** 정작 필요한
연결/오류 로그가 사라진다. 그래서:

- **RAW 수신**: 5초마다 "N배치 / M샘플 (xx.xHz)" 요약만
- **판정**: 상태가 바뀌는 순간(정상↔오조작)은 **항상** 남기고, 변화 없으면 10초 하트비트
- **반복 문자열**: `lastTextMessage`와 비교해 바뀔 때만. 펌웨어가 `MODULE_ERR`을 초당 수십 회 쏜다.

원칙: **주기적 데이터는 요약, 상태 변화는 전부.**

### 8-4. 부호 없는 정수가 없는 Kotlin

```kotlin
buf.get().toInt() and 0xFF        // uint8
buf.short.toInt() and 0xFFFF      // uint16
(cnt - lastCnt) and 0xFFFF        // wrap-around 뺄셈
```
`and 0xFF` 를 빼먹으면 128 이상이 음수가 된다. RAW 파싱 버그의 단골.

### 8-5. UI 갱신 빈도

그래프는 50Hz로 다시 그려도 되지만 **숫자 텍스트를 50Hz로 바꾸면 사람이 못 읽는다.**
`DataCollectActivity` — 그래프는 매번, 텍스트는 100ms마다.

### 8-6. 안드로이드 버전 분기 (minSdk 26, targetSdk 36)

계속 나오는 패턴이라 미리 알아둘 것:

| API | 분기 |
|---|---|
| 33 (TIRAMISU) | `registerReceiver`에 `RECEIVER_NOT_EXPORTED` 필수 |
| 33 | `writeCharacteristic`/`writeDescriptor` 시그니처 변경 (값을 인자로) |
| 34 | `startForeground`에 `FOREGROUND_SERVICE_TYPE_*` 필수 |
| 31 (S) | `PendingIntent`에 `FLAG_IMMUTABLE` 필수 |

내부 브로드캐스트는 항상 `intent.setPackage(packageName)` — 안 하면 다른 앱에 새어 나간다.

### 8-7. 헤드업 알림 조건

`NotificationChannel`의 importance가 **`IMPORTANCE_HIGH`여야** 헤드업(화면 위로 튀어나오는) 알림이 뜬다.
`IMPORTANCE_LOW`면 상단바에만 조용히 쌓인다. 그리고 포그라운드 서비스 알림(ID 1)과
경고 알림(ID 888/999)은 **ID를 다르게** 줘야 서로 덮어쓰지 않는다.

### 8-8. Chaquopy `ArrayList is not iterable`

→ §5-2. JSON 문자열로 넘겨서 우회.

### 8-9. "사용 안 하는 코드"로 보이는 함정

`CriticalActivity`는 한 세션에서 "안 쓰이는 코드"로 오판돼 삭제됐다가, 실제로는 배선만
끊겨 있던 것으로 밝혀져 복원됐다(§3-10). **삭제 전에 커밋 이력과 왜 만들어졌는지 확인할 것.**

---

## 9. 핵심 용어 빠른 사전

- **GATT**: BLE에서 데이터 주고받는 규약. "특성(Characteristic)" 단위로 읽고/쓰고/구독.
- **Notify**: 기기가 값 바뀔 때마다 폰에 자동으로 밀어주는 것 (폰이 안 물어봐도 옴).
- **CCCD (0x2902)**: "이 특성 Notify 켤게요" 하고 기기에 알리는 스위치(디스크립터).
- **MTU**: 한 번에 보낼 수 있는 최대 바이트. 기본 23(=payload 20B). RAW엔 부족 → §3-2.
- **Q31**: 소수를 정수로 표현하는 고정소수점. `값 = raw / 2^31` → 0.0~1.0.
  MCU(Cortex-M0+)에 FPU가 없어 소프트플롯을 피하려고 raw 정수로 보낸다. 환산은 폰에서.
- **little-endian**: 낮은 바이트가 먼저. ESP32-C3(RISC-V) 네이티브. `ByteOrder.LITTLE_ENDIAN`.
- **Foreground Service**: 화면 꺼도 죽지 않는 백그라운드 작업 (상단 알림 필수).
- **Chaquopy**: 안드로이드 앱 안에 Python 인터프리터를 넣어 돌리는 도구.
- **캘리브레이션(연속형 개인화)**: 사용자가 30초 주행하면, 그 사람의 `accel_active_p90`
  (자주 밟는 세기의 상위 90퍼센타일) + 고정 오프셋(0.20)으로 `accel_high`를 직접 계산하는 방식.
  discrete 3단계(강/보통/약) 분류를 대체함(2026-07-13).
- **percentile(p90)**: 값을 정렬했을 때 하위 90%가 그 값 이하인 지점. 평균보다 "이 사람이
  세게 밟는 정도"를 잘 반영(튀는 값에 덜 흔들림).
- **ECDSA**: 타원곡선 기반 전자서명 알고리즘. 개인키로 서명, 공개키로 검증만 가능(복호화 아님)
  → 증거자료의 무결성 증명에 쓰임(§7).
- **AndroidKeyStore**: 개인키를 하드웨어 보안 영역에 저장해 **앱 프로세스에서도 키 바이트를
  직접 못 꺼내게** 하는 시스템. 서명 연산 자체를 대행해준다.

---

## 10. 개발 환경 노하우

- **Gradle wrapper가 없다.** 빌드는 Android Studio로. 커맨드라인 `./gradlew` 불가.
- **`adb`는 쓸 수 있다.** 로그: `adb logcat -s BleService:* DataCollectActivity:*`
- **수집 CSV 회수**:
  ```bash
  adb shell "run-as com.example.agproject ls files/logs"
  adb shell "run-as com.example.agproject cat files/logs/pedal_....csv" > out.csv
  ```
  (앱 안에서 "로깅 데이터 추출" 버튼으로도 서명 포함 zip을 바로 공유할 수 있다 — §7-3)
- **Python 프로토타입**: `cd prototype && uv run python src/run.py` (pip 직접 호출 금지)
- **서명 검증 스크립트 단독 실행**:
  ```bash
  cd prototype && uv run python verify_signature.py pedal_<ts>.csv
  ```

---

## 11. 지금 미완성 / 주의

- `RAW_DATA` UUID(`...0004`): **임시값.** 확정되면 펌웨어 `BLE.h`와 앱 `companion object` 동시 교체.
- `WINDOW_SIZE = 50`: 0.25초. 근거 없는 초기값, 튜닝 대상.
- `ACCEL_HIGH_OFFSET = 0.20`: 개발자 1인 실기기 테스트 기준 조정값. **여러 사용자·다양한 실제
  주행 상황(고속도로 정속 주행 등)에서도 오탐/누락 없이 적절한지는 아직 미검증.**
- `brake_low`/`high_ratio`는 아직 개인화 안 됨 — `calibration.py`에서 고정값(`0.10`/`0.50`) 그대로 사용.
- `handleTextMessage`: `MODULE_ERR` 하나만. 에러 구조 개편 예정(`SD_SMALL` 등).
- `isCapacityDialogShowing`: 용량 관련은 새 에러 구조와 함께 재정비 예정.
- **공개키 자체의 진정성 증명 미구현** — §7-6 참고.
- `judge.py`/`calibration.py`/`features.py`/`verify_signature.py` 이중 관리: 두 벌 수동 동기화.
  심볼릭 링크나 Gradle 복사 태스크로 개선 여지.
- `judge.py`의 `PROFILES`/`judge()`/`judge_json()`/`thresholds_json()`은 **PC 프로토타입 데모
  (`run.py`)에서만 남아있음** — 앱의 실시간 판정 경로에선 안 쓰인다. 헷갈리지 말 것.

---

## 12. 공부 추천 순서

1. **§1 그림** 외우기 (채널 3개 방향, 200Hz→50Hz 배치, 캘리브레이션 전엔 판정 안 함).
2. `BleService.kt`를 **§3 순서대로** 따라 읽기.
   - §3-2(MTU 순서), §3-3(큐 구독 체이닝)은 BLE 특유의 함정이라 꼭 이해.
   - §3-5~3-7(캘리브레이션/판정 스킵/CSV 스키마)은 이 프로젝트의 핵심 설계.
3. `prototype/src/run.py` 실행 → discrete `PROFILES` 임계값 바꿔가며 결과 변화 관찰(데모용).
4. **§4 판정기** — `judge_with_thresholds`가 코어이고, `judge`/`judge_calibrated_json`은 둘 다
   그 위의 얇은 래퍼라는 관계를 이해할 것.
5. **§7 증거자료화** — 서명/검증 흐름, "암호화 아니라 서명인 이유".
6. **§8 함정 사전** 전체. 코드 짜기 전에 한 번, 물린 다음에 한 번 더.
7. `DataCollectActivity` + `PedalGraphView` — Activity↔Service 통신 패턴(Intent action / broadcast) 익히기.

---

## 13. 함수/메서드 사전 — "이 이름이 뭘 하는지" 전부

목적: 코드를 안 보고 이 표만 봐도 **어떤 함수를 만들어야 하는지 목록**이 나오게.
직접 짤 때는 이 이름/역할대로 함수를 하나씩 채워나가면 된다.

### `BleService.kt`

| 함수 | 역할 |
|---|---|
| `onCreate()` | 서비스 생성 1회 실행. 알림 채널 생성, `BluetoothAdapter` 획득, Chaquopy 런타임 시작 |
| `onStartCommand(intent, flags, startId)` | 서비스의 **다목적 진입점**. action 분기(에러/용량/라벨/스트림/**캘리브레이션 시작·초기화**) 먼저 처리, 없으면 연결 시작. 연결 시작 시 이전 캘리브레이션 값을 prefs에서 복원 |
| `handleErrorClearSequence()` | `0xAA` 전송 후 210ms 뒤 에러 다이얼로그 재표시 플래그 해제 |
| `handleCapacityClearSequence()` | `0xAB` 전송 후 210ms 뒤 용량 다이얼로그 재표시 플래그 해제 |
| `onDestroy()` | 서비스 종료. **GATT 해제 → CSV 닫기** 순서 고정(반대로 하면 좀비 콜백이 파일을 되살림) |
| `onBind()` | `null` 반환 — bound service 안 씀. `startService`/`startForegroundService`로만 제어 |
| `startTargetScan()` | 저장된 MAC 주소 하나만 필터링해서 BLE 스캔 시작 |
| `scanCallback.onScanResult()` | 기기 발견 → 스캔 중지 → `connectToDevice()` |
| `connectToDevice(device)` | `device.connectGatt()` 호출, 결과는 `gattCallback`으로 옴 |
| `gattCallback.onConnectionStateChange()` | 연결/해제 통지. 연결되면 `discoverServices()`, 끊기면 재스캔 |
| `gattCallback.onServicesDiscovered()` | TX/RAW characteristic 탐색 → 구독 큐에 적재 → `requestMtu()` |
| `gattCallback.onMtuChanged()` | MTU 협상 끝 → connection priority HIGH 요청 → `subscribeNext()` 시작 |
| `gattCallback.onDescriptorWrite()` | 구독(디스크립터 쓰기) 완료 콜백 → 다음 대상 구독 |
| `gattCallback.onCharacteristicChanged()` | notify 수신 시 UUID로 분기: TX→`handleTextMessage`, RAW→`handleRawData` |
| `subscribeNext(gatt)` | 큐에서 특성 하나 꺼내 구독 요청. 큐가 비면 "구독 완료" 로그만 남기고 끝 |
| `handleTextMessage(msg)` | TX 문자열 파싱. 직전과 같은 메시지면 로그 생략, `MODULE_ERR` 등 케이스 분기(오조작은 여기 안 옴) |
| `onModuleError()` | 에러 다이얼로그가 이미 떠 있으면 무시(중복 억제), 아니면 음성+알림 트리거 |
| `ensureCsvWriterLocked()` | 첫 RAW 샘플 도착 시점에 세션 CSV 파일을 lazy 생성, 새 스키마 헤더 기록(`csvLock` 보유 상태에서만 호출) |
| `readCalibratedThresholds()` | prefs에서 캘리브레이션 JSON 읽기 |
| `applyCalibratedThresholds(json)` | `calibratedThresholdsJson`과 `currentAccelHigh`를 **항상 같이** 갱신 |
| `startCalibration()` | 캘리브레이션 버퍼 비움, `isCalibrating=true`, 기존 캘리브레이션 값 즉시 null화 |
| `finishCalibration()` | Python `calibrate_thresholds_json` 호출 → 적용+prefs 저장 → `ACTION_CALIBRATION_DONE` 브로드캐스트 |
| `writeRawCsv(...)` | CSV 한 줄 기록(락 보유). **판정 윈도우당 1행**, 새 스키마(date,time,brake,accel,module_err,pedal_err,accel_exceed,label) |
| `flushRawCsv()` / `closeCsvWriter()` | 버퍼 flush / 닫기 + 이후 재생성 방지 플래그 세팅 |
| `handleRawData(data)` | **RAW 파싱 핵심.** count 읽기 → 길이 검증 → 샘플별 Q31 디코딩 → cnt 누락 검출 → (캘리브레이션 중이면 별도 버퍼 적재 + 시간 다 되면 자동 종료) → 그래프 브로드캐스트 → 윈도우 차면 `judgeWindow()` 호출 |
| `judgeWindow(window)` | 캘리브레이션 중이거나 임계값 미설정이면 **판정 자체를 건너뜀**. 아니면 `judge_calibrated_json` 호출 → 결과 파싱 → `misop`이면 `onPedalMisoperation()` |
| `onPedalMisoperation()` | 오조작 경고 트리거: 음성("PEDAL") + 헤드업 알림 + `showCriticalNotification` + `CriticalActivity` 직접 실행 |
| `playVoiceFile(type)` | 저장된 성별 설정으로 MP3 리소스 매핑 후 `MediaPlayer` 재생(`"PEDAL"`/`"SD"`/`"ERROR"`) |
| `startForegroundServiceNotification/updateNotification/createNotification/createNotificationChannel` | 포그라운드 서비스 알림 인프라 |
| `showCriticalNotification(title, content)` | `CriticalActivity`로 가는 fullScreenIntent 알림(ID 888) |
| `disconnectGatt()` | GATT 연결 해제 + close |
| `saveStatus(msg)` | 연결 상태 문자열을 `SharedPreferences`에 저장(Activity가 읽어감) |
| `sendBroadcastToActivity(action)` | 패키지 한정 브로드캐스트 전송 |
| `writeToRx(bytes)` | RX 특성에 쓰기(폰→기기 전송은 전부 이 함수 하나로) |
| `enableNotification(gatt, char)` | CCCD 디스크립터에 write해서 notify 활성화(SDK 버전 분기) |
| `handleUuidMismatch()` | 목표 서비스/특성 못 찾음 → 상태 저장 + 브로드캐스트 + 지연 종료 |
| `showHeadsUpNotification(title, content)` | 화면 위로 튀어나오는 일반 알림(importance HIGH 채널 필요) |
| `getPendingIntent()` | 알림 클릭 시 `MainActivity`로 가는 `PendingIntent` 생성 |

### `judge.py`

| 함수 | 역할 |
|---|---|
| `get_thresholds(profile)` | `PROFILES` 딕셔너리에서 프로필 조회, 없으면 `normal`로 fallback (prototype 데모용) |
| `thresholds_json(profile)` | 위 결과를 JSON 문자열로 (prototype 데모용) |
| `judge_with_thresholds(samples, thresholds, label)` | **판정 코어.** 임계값 dict 하나로 "엑셀 높고 브레이크 낮음" 비율 계산 → `misop` 결정 |
| `judge(samples, profile="normal")` | `get_thresholds(profile)`를 얹은 레거시 래퍼 |
| `judge_json(samples_json, profile)` | 위 JSON 어댑터 (prototype `run.py` 데모용) |
| `judge_calibrated_json(samples_json, thresholds_json)` | **앱이 실제로 쓰는 경로.** 캘리브레이션 임계값 JSON을 그대로 받아 `judge_with_thresholds` 호출, label은 `"personalized"` 고정 |

### `calibration.py`

| 함수 | 역할 |
|---|---|
| `calibrate_thresholds(samples)` | `extract_features`로 `accel_active_p90` 추출 → `+ ACCEL_HIGH_OFFSET`으로 `accel_high` 계산, `brake_low`/`high_ratio`는 고정값 |
| `calibrate_thresholds_json(samples_json)` | 위의 Chaquopy 경계 어댑터 |

### `features.py`

| 함수 | 역할 |
|---|---|
| `extract_features(samples, sample_rate_hz=200)` | 세션 전체 통계 + "밟고 있을 때만" 조건부 통계(`accel_active_*`) + 조작 빈도/지속시간(§4-3 표) 11종 반환 |

### `DataCollectActivity.kt`

| 함수 | 역할 |
|---|---|
| `liveReceiver.onReceive()` | 50Hz 배치 브로드캐스트 수신 → 그래프에 push, 텍스트는 100ms 스로틀로 갱신 |
| `onCreate()` | 뷰 바인딩, 버튼 리스너 등록, 기존 캘리브레이션 값 표시 |
| `onResume()` / `onPause()` | 리시버 등록/해제 + `liveStream` on/off(화면 벗어나면 라벨링도 강제 종료) |
| `startCalibrationFlow()` | `ACTION_START_CALIBRATION` 전송, 30초 카운트다운 UI |
| `onCalibrationDone(thresholdsJson)` | `ACTION_CALIBRATION_DONE` 수신 → 그래프 임계선 갱신 + 토스트 |
| `showExistingCalibration()` / `applyThresholdsToGraph(json)` | prefs의 캘리브레이션 값(또는 기본값)을 그래프에 반영 |
| `confirmResetCalibration()` / `resetCalibration()` | 확인 다이얼로그 → `ACTION_CLEAR_CALIBRATION` 전송, 그래프 기본값 복귀 |
| `exportCsvLogs()` | `filesDir/logs` 전체를 (서명 없이) zip으로 묶어 공유 — 개발자용 간이 내보내기 |
| `sendLabelToService()` / `setLiveStream()` | `BleService`에 Intent action으로 명령 전송 |
| `updateLabelUI()` | 라벨링 on/off에 따른 버튼/텍스트 색상 갱신 |

### `PedalGraphView.kt`

| 함수 | 역할 |
|---|---|
| `setThresholds(accelHigh, brakeLow)` | 임계선 값 갱신 + 다시 그리기 요청 |
| `pushBatch(accels, brakes)` | 배치 샘플을 링 버퍼에 삽입(가득 차면 덮어쓰기) |
| `clear()` | 버퍼 리셋 |
| `idx(i)` | "논리적으로 i번째(0=가장 오래된 것)" → 링 버퍼 실제 인덱스 변환 |
| `toY(v, h)` | 정규화 값(0~1) → 화면 Y 좌표(위로 갈수록 큰 값이 되도록 반전) |
| `onDraw(canvas)` | 격자 → 임계선 → accel/brake 파형 순으로 그리기 |

### `MainActivity.kt`

| 함수 | 역할 |
|---|---|
| `statusReceiver.onReceive()` | 서비스가 보낸 액션(연결됨/UUID 불일치/모듈 에러/용량 경고)에 따라 UI·팝업 갱신, 연결 성공 시 `maybeShowCalibrationOnboarding()` 호출 |
| `maybeShowCalibrationOnboarding()` | prefs에 캘리브레이션 값이 없으면 온보딩 팝업 → 확인 시 `DataCollectActivity`로 이동 |
| `onCreate()` | 배경 블러 처리, 뷰 바인딩, 버튼 리스너, 권한 확인 |
| `onResume()` / `onPause()` | 리시버 등록/해제 |
| `toggleSystem()` / `startSystem()` / `stopSystem()` | 감시 시작/정지, `BleService`에 `startForegroundService`/`stopService` |
| `updateUI()` | 등록 여부·연결 상태에 따라 카드 색상/텍스트 갱신 |
| `loadSavedData()` | `SharedPreferences`에서 등록된 타겟 정보 로드 |
| `checkPermissions()` / `checkOverlayPermission()` | SDK 버전별 필요 권한 요청 |
| `showErrorPopup()` / `showCapacityWarningPopup()` | 다이얼로그 표시. 확인 버튼 → 서비스에 clear 액션 전송 |

### `DeviceManagerActivity.kt`

| 함수 | 역할 |
|---|---|
| `listSessionTimestamps()` | `filesDir/logs`의 `pedal_<ts>.csv` 목록을 최신순으로 나열 |
| `formatSessionLabel(timestamp)` | `"20260713_032847"` → `"26/07/13-03:28:47"` 사람이 읽기 좋은 형식 변환 |
| `showSessionPickerAndExport()` / `exportSession(timestamp)` | 세션 선택 다이얼로그 → CSV+서명+공개키+검증스크립트+안내문 zip으로 묶어 공유 |
| `showSessionPickerAndDelete()` | "전체 삭제" + 개별 세션 목록 다이얼로그 |
| `confirmDeleteSession()` / `confirmDeleteAllSessions()` | 삭제 전 확인 다이얼로그 |
| `deleteSession(timestamp)` / `deleteAllSessions()` | 실제 CSV 파일 삭제 |

### `EvidenceSigner.kt`

| 함수 | 역할 |
|---|---|
| `getOrCreatePrivateKey()` | AndroidKeyStore에 EC 키 쌍이 없으면 lazy 생성(private) |
| `signFile(file)` | 파일 해시에 서명 → base64 문자열(.sig 내용) |
| `publicKeyPem()` | Keystore 인증서에서 공개키를 PEM으로 추출 |

### `CriticalActivity.kt`

| 함수 | 역할 |
|---|---|
| `setupScreenOnFlags()` | 잠금화면 위 표시 + 화면 강제 켜기 플래그 설정 |
| `onCreate()` | 경고 움짤(Glide) 재생, `btnDismiss` 리스너 등록 |
| `onBackPressed()` | 오버라이드해서 무력화 — 버튼을 눌러야만 꺼짐 |

---

## 14. Kotlin/Android 개념 사전 — 이 코드에 쓰인 기법들

구글링할 때 검색어로 그대로 쓸 수 있게 "기술 이름 → 뜻 → 여기서 왜 썼는지"로 정리.

| 개념 | 뜻 | 여기서 쓰인 이유 |
|---|---|---|
| **companion object** | 클래스에 딸린 정적(static) 멤버 묶음. 인스턴스 없이 `클래스이름.멤버`로 접근 | UUID 상수, 액션 문자열, `WINDOW_SIZE`, 캘리브레이션 관련 prefs 키 등 서비스 전체가 공유하는 상수 보관 |
| **lateinit var** | "나중에 초기화하겠다"고 선언하는 non-null var. 초기화 전 접근 시 예외 | `onCreate()`에서 `findViewById`로 늦게 채우는 뷰들(`graph`, `tvLiveValues` 등) |
| **@Volatile** | 스레드 간 **가시성**만 보장(원자성 아님). 한 스레드가 쓴 값을 다른 스레드가 캐시된 옛 값이 아니라 즉시 보게 함 | `currentLabel`, `liveStreamEnabled`, `calibratedThresholdsJson`, `currentAccelHigh`, `isCalibrating` — BLE 콜백 스레드가 읽고 다른 스레드가 씀(§8-1) |
| **synchronized(lock) { ... }** | 블록 전체를 한 번에 한 스레드만 실행하게 잠금 | `csvLock`으로 `BufferedWriter` 접근을 감쌈(여러 바인더 스레드에서 동시에 올 수 있어서) |
| **object : X() { ... }** (익명 객체) | 인터페이스/추상클래스를 그 자리에서 구현한 이름 없는 객체 | `scanCallback`, `gattCallback`, `statusReceiver`, `liveReceiver` 전부 이 패턴 |
| **object (싱글턴 선언)** | 인스턴스가 하나뿐인 객체를 언어 차원에서 보장 | `EvidenceSigner` — 상태 없는 서명 유틸이라 싱글턴이 자연스러움 |
| **ArrayDeque** | 양쪽 끝에서 삽입/삭제 가능한 큐 | `notifyQueue` — GATT 구독을 하나씩 순서대로 처리하는 큐(§3-3) |
| **ByteBuffer / ByteOrder** | 바이트 배열을 정수/실수 등으로 해석하는 표준 라이브러리. `ByteOrder`로 리틀/빅엔디안 지정 | RAW 프레임(`int32`, `uint16`) 디코딩(§3-4) |
| **and 0xFF / and 0xFFFF** | Kotlin/Java엔 부호 없는 정수 타입이 기본이 아니라서, 부호 확장을 막는 비트마스크 관용구 | `uint8`(count), `uint16`(cnt) 파싱(§8-4) |
| **when(...)** | Java의 `switch` 대응. Kotlin에서는 값을 반환하는 표현식으로도 쓸 수 있음 | `handleTextMessage`, `onCharacteristicChanged`, UUID/액션 분기 다수 |
| **Service vs Activity** | `Activity`는 화면(생명주기가 화면 가시성에 묶임), `Service`는 화면 없이 백그라운드에서 도는 컴포넌트 | BLE 감시는 화면 꺼도 계속돼야 하므로 `BleService`는 `Service` |
| **Foreground Service + startForeground()** | 시스템이 함부로 죽이지 못하는 서비스. 대신 사용자에게 보이는 상시 알림이 **필수** | `BleService`가 백그라운드에서 안정적으로 BLE를 유지하는 방법(§2) |
| **BroadcastReceiver + Intent(action)** | "어떤 일이 일어났다"를 문자열 액션으로 전체(또는 패키지 한정)에 방송하는 pub/sub 패턴 | Service↔Activity 간 단방향 알림(`ACTION_PEDAL_MISOP`, `ACTION_CALIBRATION_DONE` 등). `setPackage(packageName)`로 앱 밖으로 새는 것 방지(§8-6) |
| **SharedPreferences** | 앱 전용 key-value 영속 저장소(파일 기반, 간단한 설정값용) | 타겟 MAC 주소, 캘리브레이션 임계값 JSON, 연결 상태 저장 |
| **PendingIntent + FLAG_IMMUTABLE** | "나중에 시스템이 대신 실행해 줄 Intent"를 캡슐화한 토큰. Android 12(S)부터 변경 가능/불가능 여부를 명시해야 함 | 알림 클릭 시 `MainActivity`/`CriticalActivity` 열기(§8-6) |
| **View + Canvas + onDraw() + invalidate()** | 커스텀 뷰를 만드는 기본 골격. `invalidate()`를 부르면 시스템이 다음 프레임에 `onDraw()`를 다시 호출 | `PedalGraphView` — 차트 라이브러리 없이 직접 그리기(§6-5) |
| **링 버퍼(circular buffer)** | 고정 크기 배열을 순환시켜 오래된 데이터를 자동으로 덮어쓰는 자료구조. `head`(다음에 쓸 위치)와 `size`(채워진 개수)로 관리 | `PedalGraphView`의 최근 5초 샘플 저장 |
| **`Build.VERSION.SDK_INT >= ...` 버전 분기** | 안드로이드 API 레벨별로 동작/시그니처가 다를 때 표준적으로 쓰는 분기 패턴 | `writeCharacteristic`, `writeDescriptor`, `startForeground`, `PendingIntent` 플래그(§8-6) |
| **try/catch로 예외 삼키기** | 실패해도 상위 흐름(판정 루프, 서비스)이 멈추면 안 되는 곳에서 의도적으로 예외를 잡아 로그만 남김 | `judgeWindow`, CSV 쓰기 — 한 번 실패해도 다음 윈도우/샘플은 계속 처리 |
| **Chaquopy `Python.getInstance().getModule(...).callAttr(...)`** | JVM에서 Python 모듈의 함수를 이름으로 호출하는 API | `judge_calibrated_json`/`calibrate_thresholds_json` 호출부(§5) |
| **AndroidKeyStore + KeyPairGenerator** | 키 생성/저장을 하드웨어 보안 모듈에 위임하는 시스템 Provider | `EvidenceSigner`가 서명용 EC 키를 앱 프로세스 밖에 안전하게 보관(§7-2) |
| **FileProvider** | 앱 전용 파일을 다른 앱(공유 시트 대상)에 안전하게 노출하는 ContentProvider | CSV/zip을 시스템 공유 시트로 내보낼 때 `content://` URI 생성(§7-3) |
| **ZipOutputStream** | 여러 파일을 하나의 zip으로 묶는 표준 라이브러리 | 증거자료 5종 파일(§7-3), 개발자용 CSV 일괄 내보내기(§6) |
| **CountDownTimer** | 지정 시간 동안 주기적으로 콜백을 주는 안드로이드 유틸(카운트다운 UI에 흔히 씀) | "패턴 파악" 30초 카운트다운 버튼 텍스트 갱신(§6-1) |

---

## 15. Python 개념 사전 — `judge.py`/`calibration.py`/`features.py`에 쓰인 것들

| 개념 | 뜻 | 여기서 쓰인 이유 |
|---|---|---|
| **`from __future__ import annotations`** | 타입 힌트를 실행 시점에 평가하지 않고 문자열처럼 지연 평가. 최신 제네릭 문법(`list[float]` 등)을 구버전 Python에서도 에러 없이 쓸 수 있게 함 | 각 파일 최상단에 선언 |
| **타입 힌트 (`list[list[float]]`, `dict[str, float]`)** | 함수 시그니처에 입출력 타입을 명시. 런타임 강제는 안 되지만 문서화 + IDE 자동완성/정적분석에 도움 | `judge_with_thresholds()`, `extract_features()`, `calibrate_thresholds()` 시그니처 |
| **`dict.get(key, default)`** | 키가 없을 때 예외 대신 기본값을 돌려주는 안전한 조회 패턴 | `get_thresholds`의 fallback(`normal` 프로필, prototype 데모용) |
| **모듈 레벨 상수 (`PROFILES`, `ACCEL_HIGH_OFFSET`, `DEFAULT_BRAKE_LOW`)** | 클래스 없이 모듈 최상단에 두는 전역 상수. 다른 모듈에서 `calibration.ACCEL_HIGH_OFFSET`처럼 바로 접근 | 임계값/오프셋 단일 소스(§4-4) |
| **`json.dumps` / `json.loads`** | 파이썬 객체 ↔ JSON 문자열 직렬화/역직렬화 | Chaquopy 경계에서 리스트/딕셔너리를 문자열로 주고받는 방법(§5-2) |
| **`json.dumps(..., ensure_ascii=False)`** | 기본값(`True`)은 비ASCII 문자를 `\uXXXX`로 이스케이프함. `False`로 하면 한글이 그대로 사람이 읽는 문자열로 나옴 | `reason` 필드에 한글 설명 포함 |
| **docstring (`"""..."""`)** | 함수/모듈 맨 앞에 오는 문자열 리터럴이 자동으로 설명서(`__doc__`)가 됨 | 계약(Contract)·개인화 구조·설계 변경 이력(discrete→연속형)을 파일 최상단에 명시 |
| **순수 함수로 설계 (side-effect 없음)** | 입력만으로 출력이 결정되고 외부 상태를 안 건드리는 함수 | `judge_with_thresholds()`, `calibrate_thresholds()`가 BLE도 안드로이드도 모르게 만들어 PC/앱 양쪽에서 동일하게 테스트 가능(§4-1) |
| **조건부 리스트 컴프리헨션 (`[x for x in samples if x[0] >= threshold]`)** | 특정 조건을 만족하는 원소만 걸러 새 리스트를 만드는 문법 | `features.py`의 "active" 통계(accel≥0.1인 샘플만) 계산(§4-3) |
| **연속 구간(run-length) 계산** | 리스트를 훑으며 조건이 연속으로 참인 구간의 길이들을 모으는 패턴 | `press_rate_per_min`/`press_duration_mean_sec` — 페달을 "한 번 밟는 사건" 단위로 묶어 세기 위함(§4-3) |
| **모듈러 역원 (`pow(x, -1, p)`)** | 모듈러 산술에서 나눗셈을 대체하는 연산(x⁻¹ mod p). Python 3.8+ 내장 `pow`가 3항 인자로 직접 지원 | `verify_signature.py`의 타원곡선 점 연산(ECDSA 검증)에서 사용(§7-4) |
| **stdlib-only 설계(외부 의존성 0)** | `pip install` 없이도 표준 라이브러리만으로 동작하게 짜는 것 | `verify_signature.py` — 검증자(제3자) 컴퓨터에 아무것도 설치 안 돼 있어도 동작해야 하므로(§7-4) |

---

> 더 깊은 규격은 `BLE_RAW_스트림_규격.md`(프레임 포맷·MTU 요구사항),
> `BLE_NUS_마이그레이션.md`(채널 설계), `진행상황_및_로드맵.md`(현재 위치) 참고.
